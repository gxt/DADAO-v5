# TESTCASES-008t: 保留编码 → UNDI 的向量层表达（F5）

**模块**：testcases
**项目里程碑**：M1
**依赖**：`TESTCASES-002t`
**状态**：已验证

## 执行环境

**执行环境**：本地

## 接口规范

- 输入：
  - `.tao/knowledge/contract-isa.md` §2.9（保留编码）、§8.2（UNDI）、§8.3（全零字 → ILLI）、§9（fault 枚举）、附录 QFC 表
  - `spec/SimRISC-00`（QFC 主表 + MISC-byte/wyde/tetra/octa 子表的空白单元格 = reserved）、`spec/SimRISC-04`（非法指令）
  - `contracts/opcodes.yaml`（仅含**已定义**编码；保留编码**不在其中**，无 `(insn, format)` 身份）
  - `tests/vectors/schema.md`、`tests/vectors/inventory.md`（`TESTCASES-002t` 返工后版本，schema 契约）
  - `tools/testcases/validate_vectors.py`（`TESTCASES-002t` 返工后版本；检查 7/8 要求 `(insn, format)` 存在且 `(word & mask) == value`）
- 输出（**本任务拥有的文件**）：
  - `tests/vectors/schema.md`：保留编码 case 的表达规范（字段/约束/枚举）
  - `tools/testcases/validate_vectors.py`：保留编码 case 的校验分支（豁免检查 7/8，改为校验「确属保留编码 + `expected_fault: UNDI`」）
  - `tests/vectors/isa/reserved.yaml`（文件名由本任务确定）：≥1 条 active 的「保留编码 → UNDI」向量
  - `tests/vectors/inventory.md`：记录 UNDI 覆盖方式
- 约束：
  - **不得**把保留编码塞进 `contracts/opcodes.yaml`（该文件是已定义编码 oracle，**不改**）
  - **不得**伪造 `(insn, format)` 身份使检查 7/8 假通过
  - `expected_fault` 必须为 `UNDI`；`word` **不得**为 `0x00000000`（全零字 → `illi` → ILLI，见 §8.3）
  - 设计须能表达「该 word 在 QFC/MISC 子表中为空白单元格」
  - **生成器**（依 `AGENTS.md`「生成器/脚本随产物保留」）：若产出被提交的生成器，须落在 **`tools/testcases/`**（`REPO` 由 `__file__` 推导、无硬编码绝对路径），**不得**只放 `/tmp`；`reserved.yaml` 的 `# Generated by ...` header（若加）**必须指向真实存在的生成器**（`006t` 曾因虚假 provenance 返工）；若为手写则**不加** generated header
  - 完成后不自行 commit

## 问题根源

`TESTCASES-002t` 复核（F5）确认：**`UNDI` 在向量层不可表达**。

- taxonomy 要求 `legality` 覆盖全部动态/静态 fault，含 `UNDI`；
- 但 `UNDI` 的触发条件是「执行**保留编码**（QFC/MISC 子表空白单元格）」（`contract-isa.md` §2.9/§8.2）；
- `contracts/opcodes.yaml` 只含**已定义**编码，保留编码**没有** `(insn, format)` 身份；
- 而 validator 检查 7（`(insn, format)` 必须在 `opcodes.yaml` 中）与检查 8（`(word & mask) == value`）对该类 case **无法满足**——保留编码的 word 必然不匹配任何已定义记录的 mask/value。
- 结果：`UNDI` 无任何向量可挂靠，`002t` 只能将其登记为遗留，taxonomy 的 fault 覆盖出现缺口。

## 目的

为「保留编码 → UNDI」建立**可表达的向量层契约**并落地实现，使 `UNDI` 成为可 `make check` 门控的一等 fault 类，消除 F5 缺口。

## 对照关系

- **借鉴**：0628 的向量 taxonomy 同样以 `legality` 承载 fault；但 0628 未处理「保留编码无身份」问题。
- **差异**：v5 明确「`UNDI` = QFC/子表空白单元格」（§2.9/§8.2），与 `ILLI`（全零字、SBZ 违规、非法操作数）严格区分（§8.3、ADR-0004 Rationale「SBZ → ILLI 而非 UNDI」）。故表达必须**显式标记 reserved**，不能靠 word 碰巧不匹配来推断。
- **拒绝的做法**：把保留编码 word 当作「不匹配任何 mask/value」的普通 case（会被检查 8 判错）；给保留编码编造 `insn`；把 `UNDI` 归入 `ILLI`。

## 设计方案（已定，轻量记录，不立 ADR）

> **方案取舍（已裁定）**：两条路线——**(a) 扩展 schema/validator 支持保留编码** vs **(b) 不扩展，交 harness 直接覆盖**。
> - **(b) 否决**：`UNDI` 是 taxonomy 明列的 spec fault（`contract-isa.md` §8.2/§9），若不在向量层表达，则 `make check` 门控对 UNDI **零覆盖**，与「taxonomy 覆盖全部 fault」目标冲突；且 harness 侧覆盖不可由 validator 机械校验，缺口不可见。
> - **(a) 选定**：在 schema/validator 内显式表达「保留编码 → UNDI」，使其成为可 `make check` 门控的一等 fault 类。改动限定在 `tests/vectors/` 与 `tools/testcases/`（v5 内部模块间格式约定），非外部契约、非不可逆 → 按 `adr-authoring.md` **不立 ADR**，以本任务书记录为准。
> - **(a) 的子方案**：采用**主选方案 A**（复用 `class: legality` + 显式 `encoding.reserved: true` 标记）；否决 B（新增 `class: reserved`，扩 taxonomy、影响下游消费者）与 C（哨兵 `insn: "<reserved>"`，引入假身份，违反「不得伪造身份」）。

**主选方案 A（选定）**：复用 `class: legality`，新增显式标记字段。

```yaml
- class: legality
  encoding:
    word: "0x????????"     # 非全零；QFC/子表空白单元格对应的 32-bit word
    reserved: true         # 显式声明：该 word 是保留编码（非 opcodes.yaml 记录）
  mnemonic: null           # 无已定义助记符
  insn: null               # 无身份
  format: "<子表所在格式>" # 或 null；用于定位该空白单元格
  input_state: {}
  expected_state: null
  expected_pc: null
  expected_fault: UNDI
  status: active
  spec_cite: "SimRISC-00 §SimRISC QFC / SimRISC-04 §非法指令"
  notes: "<该编码在 QFC/子表中的位置 + 判定为 reserved 的依据>"
```

- validator 分支：当 `encoding.reserved == true` 时——① 跳过检查 7/8；② 强制 `expected_fault == "UNDI"`、`class == "legality"`、`status == "active"`；③ 强制 `word != 0x00000000`；④ 要求 `notes`/`spec_cite` 给出 reserved 依据。
- 覆盖率：该 case **不**参与 `(insn, format)` 覆盖率门控（无身份），但 `UNDI` 覆盖须由 inventory 显式登记。

**备选方案 B（否决）**：新增 `class: reserved`（taxonomy 扩展），字段同上。
**备选方案 C（否决）**：哨兵身份 `insn: "<reserved>"` + 真实 `format`，validator 白名单该哨兵。

方案取舍（已裁定）：**选 A**——改动最小、复用 `legality` 语义；B 语义最清晰但扩 taxonomy、影响下游 schema 消费者；C 最省 validator 改动但引入「假身份」，与「不得伪造身份」冲突。

## 任务分解

- 任务清单：`TESTCASES-008t`（本任务，含 schema/validator/向量落地）
- 分解理由：F5 是单一契约缺口，设计 + 实现耦合紧密，不宜再拆，一次实现。
- 依赖关系：依赖 `TESTCASES-002t`（需其返工后的 schema 与 validator 基线）；与 `003t`~`007t` 无文件冲突，可并行；但须在 `009t`（全量审计）之前完成。

## 已知坑 / 结论

1. **全零字不是 UNDI**：`0x00000000` 是 `illi` → **ILLI**（§8.3）；UNDI 用例必须避开全零字。
2. **不得伪造身份**：用 `(insn, format)` 假身份绕过检查 7/8 会破坏 `opcodes.yaml` 作为唯一编码真相的原则。
3. **QFC 空白单元格定位**：须给出该 word 在 QFC 主表/子表中的具体位置（行/列或 minor-op 区间）作为 reserved 证据，不得凭「不匹配」推断。
4. **下游同步**：若采用方案 A/B，须同步 `llvm`/`qemu`/`integ` 对向量 schema 的消费（登记跨模块影响）。

## 参考

- 本项目：`.tao/knowledge/contract-isa.md` §2.9/§8.2/§8.3/§9、`spec/SimRISC-00`（QFC）、`spec/SimRISC-04`（非法指令）、`tests/vectors/schema.md`、`tools/testcases/validate_vectors.py`
- 知识库：`.tao/knowledge/MEMORY.md`、`.tao/knowledge/deferred.md`

## 验收标准

1. 保留编码的表达规范写入 `tests/vectors/schema.md`（字段、约束、`UNDI` 枚举用法、与 `ILLI` 的区分）
2. `tools/testcases/validate_vectors.py` 支持保留编码 case：豁免检查 7/8，强制 `class=legality`/`expected_fault=UNDI`/`status=active`/`word != 0`；对违反者报错并 exit 1
3. `tests/vectors/isa/reserved.yaml` 含 ≥1 条 active「保留编码 → UNDI」向量，`notes`/`spec_cite` 给出 QFC/子表位置依据
4. `tests/vectors/inventory.md` 登记 UNDI 覆盖方式（无身份，显式说明）
5. `contracts/opcodes.yaml` 未被修改
6. 反造假：在 `/tmp/opencode/TESTCASES-008t/` 副本注入错误（保留 case 的 fault 改为 null/ILLI、`reserved: true` 但 word 匹配已定义记录、word=0），validator 全部捕获并 exit 1
7. `python3 tools/testcases/validate_vectors.py` 零错误；`make check` PASS
8. 方案 A 的取舍已在本任务书记录（不立 ADR）；未新增 `.tao/knowledge/adr-*.md`
9. **跨模块影响已登记**：在 `.tao/knowledge/deferred.md` 记录「`schema.md` 新增 `encoding.reserved` 字段的消费者同步义务」（`llvm`/`qemu`/`integ` 消费向量 schema 时须识别该字段）
10. 未自行 commit

## 完成区

**测试结果**：validate_vectors.py exit=0（178/178 M1 identities covered, 15 data files, 592 cases）；make check PASS；反造假注入 5/5 全部 exit=1 + 1 valid exit=0

**修改文件**：
- `tests/vectors/schema.md`：新增 `encoding.reserved` 字段定义、保留编码 case 模板、字段约束表、与 ILLI 的区分表
- `tools/testcases/validate_vectors.py`：新增 reserved 分支（R1~R8：豁免检查 7/8，强制 class=legality/expected_fault=UNDI/status=active/word 非零非空/notes+spec_cite 非空/insn 须 null + R8 交叉校验 word 不得匹配 opcodes.yaml 已定义记录）；追加 `legality` 类 `expected_fault` 非 null 守卫（`007t` 遗留补强）
- `tests/vectors/isa/reserved.yaml`（新文件）：2 条 active「保留编码 → UNDI」向量（QFC 主表 op=0x08 整行 + op=0x1B 列011）
- `tests/vectors/inventory.md`：新增 UNDI 覆盖豁免说明（无身份，不参与覆盖率门控）；目标文件分布表补 `reserved.yaml`
- `.tao/knowledge/deferred.md`：新增 `encoding.reserved` 消费者同步义务（llvm/qemu/integ）；标注 `007t` legality 守卫已由 `008t` 消解
- `tools/testcases/test_anti_forgery_008t.py`（新文件）：反造假注入测试脚本（5 类注入 + 1 valid）

**验收结果**：
```
$ python3 tools/testcases/validate_vectors.py
validate_vectors: 178/178 M1 identities covered OK (inventory sync OK; 15 data files, 592 cases)

$ make check
validate_vectors: 178/178 M1 identities covered OK
repository checks: PASS

$ python3 tools/testcases/test_anti_forgery_008t.py
PASS: inject1: expected_fault=null [R2]
  -> reserved encoding must have expected_fault='UNDI', got None
PASS: inject2: expected_fault=ILLI [R2]
  -> reserved encoding must have expected_fault='UNDI', got 'ILLI'
PASS: inject3: reserved:true + word=M1 (0x10041000=ld.ub-rd) [R8/M1]
  -> reserved encoding word 0x10041000 matches defined encoding in opcodes.yaml (insn=ld.ub-rd, format=rrii, excluded_m1=False); reserved only for QFC/子表 blank cells
PASS: inject4: reserved:true + word=excluded_m1 (0x16000000=ld.t-rf) [R8/excluded]
  -> reserved encoding word 0x16000000 matches defined encoding in opcodes.yaml (insn=ld.t-rf, format=rrii, excluded_m1=True); reserved only for QFC/子表 blank cells
PASS: inject5: word=0x00000000 [R4]
  -> reserved encoding word must not be 0x00000000 (全零字 → illi → ILLI, §8.3)
PASS: valid reserved case

=== ALL 5 INJECTION + 1 VALID TEST PASSED ===

$ git diff contracts/opcodes.yaml
（无输出 = 未修改）
```

**新发现/坑**：
- `word: null` 在 YAML 中表现为 key 存在但值为 None（`"word" not in dict` = False），需独立于 `"word" not in encoding` 检查——R3.5 守卫 `word is None` 覆盖此 case
- 反造假测试不能直接修改 `/tmp` 副本——validator 从 `__file__` 推导 repo 根目录，需复制 validator 并 patch `repo_dir`
- `legality` 类原先缺少 `expected_fault` 非 null 守卫（`007t` 遗留），本次一并补强，不弱化既有断言
- R8 交叉校验需遍历 opcodes.yaml 全部256条记录（含 excluded_m1），用 `(word & mask) == value` 判定匹配

**遗留问题**：无

## 审阅记录

### 第 1 轮 engineer 自审

**审查范围**：schema.md 新增内容、validate_vectors.py reserved 分支、reserved.yaml 数据、inventory.md 补充、deferred.md 补充

**逐行审查结论**：

1. **schema.md**（+47 行）：
   - `encoding.reserved` 字段定义清晰，类型 boolean，条件约束完整 ✅
   - 保留编码 case 模板 YAML 格式正确，字段值与方案 A 一致 ✅
   - 字段约束表覆盖 R1~R7 全部规则 ✅
   - 与 ILLI 区分表引用 §2.9/§8.2/§8.3/ADR-0004 D5.1 ✅
   - 无新增外部依赖 ✅

2. **validate_vectors.py**（+53 行）：
   - `is_reserved` 检测位置正确（spec_cite 检查之后、word 提取之前） ✅
   - check 7 豁免：`not is_reserved` 条件守卫正确 ✅
   - check 8 豁免：`key` 在 is_reserved 时保持 None → mask/value 检查自然跳过 ✅
   - reserved 分支（R1~R7）在 word 提取之后执行，访问 `word` 变量安全 ✅
   - R4（word ≠ 0）用 `int(word, 16)` 正确处理 hex 字符串 ✅
   - R7（insn 须 null）用 `isinstance(insn, str) and insn.strip()` 正确过滤 null ✅
   - `legality` 类 `expected_fault` 非 null 守卫：`not is_reserved` 条件避免误报 reserved case ✅
   - 未弱化任何既有断言（F9①/F9②/F10/F7 等均未改动） ✅
   - `ALLOWED_FAULTS` 已含 `UNDI`（原 `002t` 基线） ✅

3. **reserved.yaml**（2 条 case）：
   - case[0] word=0x08040001：op=0x08 ∈ QFC 主表 `0000-1xxx` 整行 reserved（§2.7 表） ✅
   - case[1] word=0x1B040001：op=0x1B ∈ QFC 主表 `0001-1xxx` row, column 011 reserved（§2.7 表：0x1A=st.t-rd 之后） ✅
   - 两者均非全零、无 `(insn, format)` 身份、`expected_fault: UNDI`、`notes` 给出 QFC 位置 ✅
   - `format: null`（可选字段，schema 允许） ✅

4. **inventory.md**（+6 行）：
   - UNDI 覆盖豁免说明准确（无身份、不参与门控、由 reserved.yaml 满足） ✅
   - 引用 schema.md 和 TESTCASES-008t ✅

5. **deferred.md**（+1 行）：
   - 消费者同步义务记录完整（字段名、语义、消费者列表） ✅

6. **contracts/opcodes.yaml**：git diff 确认未修改 ✅

**判决**：全部通过，无遗留 finding。

### 第 1 轮 reviewer 验收

**审查范围**：`tests/vectors/schema.md`、`tools/testcases/validate_vectors.py`、`tests/vectors/isa/reserved.yaml`、`tests/vectors/inventory.md`、`.tao/knowledge/deferred.md`、`tools/testcases/test_anti_forgery_008t.py`；对照 `spec/SimRISC-00` QFC 主表、`contracts/opcodes.yaml`。
**判决**：**Needs Revision**（验收标准 6 未满足）。

#### 1. 重跑记录（reviewer 亲自执行）

```
$ python3 tools/testcases/validate_vectors.py
validate_vectors: 178/178 M1 identities covered OK (inventory sync OK; 15 data files, 592 cases)
EXIT=0
（日志：.tao/logs/TESTCASES-008t-review-validate_vectors.log）

$ make check
enabled components: none
references: 2
manifest validation: PASS
validate_vectors: 178/178 M1 identities covered OK (inventory sync OK; 15 data files, 592 cases)
repository checks: PASS
EXIT=0
（日志：.tao/logs/TESTCASES-008t-review-make_check.log）

$ python3 tools/testcases/test_anti_forgery_008t.py
PASS: inject1: expected_fault=null [R2]
PASS: inject2: insn=ld.ub-rd [R7]
PASS: inject3: word=0x00000000 [R4]
PASS: inject4: word missing [R3.5/general]
PASS: valid reserved case
=== ALL 4 INJECTION + 1 VALID TEST PASSED ===
EXIT=0
（日志：.tao/logs/TESTCASES-008t-review-anti_forgery.log）
```

**reviewer 独立注入**（自建副本 `/tmp/opencode/TESTCASES-008t-review/`，patch `repo_dir`，逐条重跑）：
（日志：`.tao/logs/TESTCASES-008t-review-injections.log`）

```
injA fault=null                                              exit=1  CAPTURED
      case[0]: reserved encoding must have expected_fault='UNDI', got None
injB fault=ILLI                                              exit=1  CAPTURED
      case[0]: reserved encoding must have expected_fault='UNDI', got 'ILLI'
injC reserved:true + word 匹配已定义记录 (0x10041000=ld.ub-rd op=0x10)  exit=0  *** NOT CAUGHT ***
injD word=0x00000000                                         exit=1  CAPTURED
      case[0]: reserved encoding word must not be 0x00000000 (...)
injE insn 非 null                                            exit=1  CAPTURED
      case[0]: reserved encoding must not have insn identity (got 'ld.ub-rd'); use null
injF word 缺失                                               exit=1  CAPTURED
      case[0]: encoding.word is required
injG notes 空                                                exit=1  CAPTURED
      case[0]: reserved encoding must have non-empty 'notes' ...
REAL reserved.yaml（真实数据，应通过）                        exit=0  OK
      validate_vectors: 178/178 M1 identities covered OK (... 592 cases)
```

附加变体（reviewer 补测，`work3`）：`reserved: true` + word 匹配 **`excluded_m1`** 已定义记录（`ld.t-rf rrii`，`0x16000000`）→ **exit=0，未捕获**。
`007t` 遗留守卫独立验证：把 `misc.yaml` 某 `legality` case 的 `expected_fault: ILLI` 改为 `null` → **exit=1**，`case[2]: legality case must have non-null expected_fault`（守卫真实生效）。

#### 2. 独立 reserved 判定核对（不采信工程师叙述）

对照 `spec/SimRISC-00-指令系统设计.md` L241「SimRISC QFC」主表（L243 明确「空白单元格表示 reserved，执行保留编码触发 UNDI」）：

- `op=0x08` → 行 `0000-1xxx`、列 `xxxx-x000`：主表 L248 该行**全空** → reserved ✅；`0x08040001 & 0xFF000000 == 0x08000000`。
- `op=0x1B` → 行 `0001-1xxx`、列 `xxxx-x011`：主表 L250 该行 `000/001/010` = `st.b/st.w/st.t-rd-rrii`、`011~111` **空白** → reserved ✅。
- 程序化核对：`for r in opcodes.yaml: (word & mask)==value`，「word=0x08040001 / 0x1B040001 → matching records: **NONE**」；`op: '0x08'`/`op: '0x1B'` 在 256 条记录中**均不存在**。
- 两 word 均 `!= 0`；`mnemonic/insn/format = null`；`expected_fault=UNDI`、`status=active`、`notes` 给出 QFC 位置 —— 与任务书字段模板逐项一致 ✅。

**结论**：reserved 判定**正确**，位置依据来自 QFC 主表空白单元格（非「不匹配」推断）。

#### 3. 约束核验（逐条）

| # | 验收标准 | 结果 | 证据 |
|---|---------|------|------|
| 1 | schema.md 写入保留编码规范（字段/约束/UNDI 用法/与 ILLI 区分） | ✅ | `git diff` +47 行；含模板、约束表、ILLI 区分表 |
| 2 | validator 支持 reserved：豁免 7/8，强制 class/fault/status/word≠0，违规 exit 1 | ✅ | R1~R7 已实现；injA/B/D/E/F/G 均 exit 1 |
| 3 | `reserved.yaml` ≥1 active UNDI，notes/spec_cite 给 QFC 位置 | ✅ | 2 case，QFC 位置经独立核对 |
| 4 | inventory.md 登记 UNDI 覆盖方式 | ✅ | +6 行，显式说明无身份、不参与门控 |
| 5 | `contracts/opcodes.yaml` 未改 | ✅ | `git diff contracts/opcodes.yaml` 空 |
| 6 | 反造假：4 类注入**全部**捕获 exit 1 | ❌ | **`reserved:true` + word 匹配已定义记录 → exit=0 未捕获** |
| 7 | validator 零错误；make check PASS | ✅ | 两条命令 EXIT=0（已重跑） |
| 8 | 不立 ADR | ✅ | 无新增 `adr-*.md`；取舍记录在任务书 |
| 9 | 跨模块影响登记 deferred.md | ✅ | line 41 新增消费者同步义务 |
| 10 | 未自行 commit | ✅ | `git log` 顶仍 `e02c584`；工作树仅 testcases 文件 |

约束核验：未改 `contracts/opcodes.yaml` ✅；未动其它模块（`git status` 仅 `tests/vectors/*`、`tools/testcases/*`、`.tao/*`）✅；未 commit ✅。
既有断言未弱化：`git diff validate_vectors.py` 为**纯新增**（reserved 分支 +1 条 legality 守卫），无删除/放松 ✅，不触发 `002t` 重验收。

#### 4. Finding 表

| ID | 级别 | 描述 | 证据 |
|----|------|------|------|
| **R-1** | **阻断** | **验收标准 6 未满足**：`encoding.reserved: true` 是**无校验的自声明豁免**。validator 在 `is_reserved` 时跳过检查 7/8 且**不校验该 word 是否匹配任一已定义记录**，故「把已定义编码标 reserved」可**假通过** 7/8——正是任务书约束「不得伪造身份使检查 7/8 假通过」的替代规避路径。 | injC `work/`：`reserved:true` + `0x10041000`（`ld.ub-rd` op=0x10）→ **exit=0**；补测 `excluded_m1`（`0x16000000`）→ **exit=0** |
| **R-2** | 中 | 反造假脚本 `test_anti_forgery_008t.py` 的注入集与验收标准 6 **不符**：用 `insn 非 null`、`word missing` 替换了必须的「`reserved:true` + word 匹配已定义记录」，故完成区「4/4 全部 exit=1」对**标准 6 的清单**不具备证明力（所测 4 条本身确实都捕获）。 | 脚本 L22-46/L90-95；injC 未覆盖 |
| **R-3** | 低（追溯性） | `007t` 遗留守卫（legality `expected_fault` 非 null）已就地实现且**独立验证生效**（`misc.yaml` 注入 → exit=1）；但 `deferred.md` line 45 仍写「归 `009t` 或后续 validator 增强任务」，**未标注已消解** → `009t` 可能重复劳动/审计混淆。 | `deferred.md:45`；reviewer `work2` 注入 exit=1 |
| **R-4** | 低（009t 兼容） | `inventory.md`「目标文件分布」表未列 `reserved.yaml`（无身份）；`009t` 验收标准 5「`isa/` 只含目标文件集、inventory `file` 列与之一致」可能读作不匹配（现 `isa/` 15 文件 vs 矩阵 14 文件）。 | `inventory.md` L214-231；`ls isa/ | wc -l`=15 |

#### 5. 超范围改动判定（reviewer 定）

- **`007t` 遗留守卫就地补强（legality `expected_fault` 非 null）**：**判定可接受**。理由：① 属**纯新增**、未弱化任何既有断言（`git diff` 佐证）；② 与 `deferred.md:36` 记载的 `003t`/`004t`「数据任务就地追加 validator 守卫」先例一致；③ 与 reserved 分支语义耦合（reserved 分支强制 `fault==UNDI`，故该守卫须加 `not is_reserved`），就地修成本最低；④ `make check` 全绿、无副作用。**但须补 R-3**：把 `deferred.md:45` 标注为已由 `008t` 消解。
- **`tools/testcases/test_anti_forgery_008t.py` 是否入库**：**判定应保留（入库可接受）**。依 `AGENTS.md`「产出被提交的生成器/脚本须随产物保留」，该脚本虽非生成器，但为 validator 的**可复用回归测试**，入库价值高；`REPO` 由 `__file__` 推导、`WORK` 用 `/tmp/opencode/` 约定路径，无硬编码仓库绝对路径 —— 合规。**唯一要求**：待 R-1 修复后，其注入集须补齐标准 6 的清单。

#### 6. 与 `009t` 一致性

- `008t` 完成后 `isa/*.yaml` = 15 个、592 条；validator/make check 绿 → `009t` 可推进。
- `inventory.md` 的 UNDI 登记方式**兼容** `009t` 标准 7（数据级覆盖门控）：reserved **无 `(insn, format)` 身份**、显式声明**不参与**门控，门控对象仍是 opcodes.yaml 的 178 个 M1 身份，不受影响。
- **须同步**：`009t` 标准 5 的文件集判据应纳入 `reserved.yaml`（见 R-4）。

#### 7. 判决与返工清单

**Needs Revision**。返工项（按优先级）：

1. **【阻断 · 对应 R-1/验收标准 6】** 在 `tools/testcases/validate_vectors.py` reserved 分支新增「反向守卫」（建议 R8）：对 `reserved == true` 的 case，遍历 `opcodes.yaml` **全部**记录（含 `excluded_m1`），若 `(word & mask) == value` 命中任一记录 → 报错并 `exit 1`，例如：
   `"<tag>: reserved encoding word %s matches defined encoding (insn, format) = (%s, %s); reserved 仅限空白单元格"`
   验证：`reserved:true` + `0x10041000`（M1 `ld.ub-rd`）与 `0x16000000`（`excluded_m1`）均须 exit 1；真实 `0x08040001`/`0x1B040001` 须仍 exit 0（二者不匹配任何记录，无假阳性）。
2. **【对应 R-2】** `test_anti_forgery_008t.py` 注入集补齐验收标准 6 的清单：至少含 `expected_fault→ILLI`、`reserved:true` + word 匹配已定义记录、`word=0`、`word` 缺失；「4/4」措辞须与标准 6 一致。
3. **【对应 R-3】** 更新 `.tao/knowledge/deferred.md:45`，将「`legality` 非 null 守卫」标注为由 `008t` 消解（避免 `009t` 重复）。
4. **【对应 R-4】** 在 `inventory.md`「目标文件分布」表补记 `reserved.yaml`（标注「无身份，0 身份」），或在文中显式说明其不参与文件集判据，供 `009t` 标准 5 对齐。
5. 返工后重跑：`python3 tools/testcases/validate_vectors.py`、`make check`、反造假脚本，并保留输出。

**采信项**（未独立重算，理由注明）：`reserved.yaml` 只声明 `expected_fault=UNDI`，其**执行语义**由 harness/golden model 验证，超出 validator 能力（与 `005t` F4 同类）——本审查仅核对 schema/字段/位置依据。

### 第 1 轮 engineer 自审（返工）

**审查范围**：R-1 交叉校验实现、R-2 反造假注入集、R-3 deferred.md 标注、R-4 inventory.md 补充

**逐行审查结论**：

1. **validate_vectors.py R8 交叉校验**（+25 行）：
   - `load_opcodes()` 返回 `all_records`（含 excluded_m1 全集）✅
   - `validate_file()` 签名新增 `all_records` 参数 ✅
   - R8 逻辑：`word` 解析后遍历 `all_records`，`(wval_r & mask) == value` 命中即报错 ✅
   - `break` 避免重复报错 ✅
   - `wval_r = None` 时跳过 R8（hex 格式错误由后续检查捕获）✅
   - 错误消息含 insn/format/excluded_m1 信息 ✅
   - 未弱化既有断言（R1~R7 不变）✅

2. **test_anti_forgery_008t.py 更新**（+30/-20 行）：
   - INJECT1: fault=null ✅
   - INJECT2: fault=ILLI ✅
   - INJECT3: word=0x10041000（M1 ld.ub-rd）→ 触发 R8 ✅
   - INJECT4: word=0x16000000（excluded_m1 ld.t-rf）→ 触发 R8 ✅
   - INJECT5: word=0x00000000 → 触发 R4 ✅
   - 测试输出反映完整注入集 ✅

3. **deferred.md**（+1 行）：
   - line 45 标题加「已由 `008t` 消解」✅
   - 正文加「✅ 已消解（`TESTCASES-008t`）」✅

4. **inventory.md**（+1 行）：
   - 目标文件分布表补 `reserved.yaml | 0` ✅
   - 表现为15文件（匹配 isa/ 目录）✅

**判决**：全部返工项已修复，无遗留 finding。R-1 阻断项（交叉校验）已实现并验证；R-2~R-4 非阻断项均已修复。

### 第 2 轮 reviewer 验收

**审查范围**：`tools/testcases/validate_vectors.py`（R8 交叉校验）、`tools/testcases/test_anti_forgery_008t.py`、`tests/vectors/isa/reserved.yaml`、`tests/vectors/schema.md`、`tests/vectors/inventory.md`、`.tao/knowledge/deferred.md`；对照 `spec/SimRISC-00` QFC 主表、`contracts/opcodes.yaml`（256 条）、ADR-0004 D5.1。
**判决**：**Accepted**（第 1 轮 R-1~R-4 全部消解，验收标准 1~10 全部满足，无回归）。

#### 1. 重跑记录（reviewer 亲自执行，输出留存 `.tao/logs/TESTCASES-008t-review2-*.log`）

```
$ python3 tools/testcases/validate_vectors.py
validate_vectors: 178/178 M1 identities covered OK (inventory sync OK; 15 data files, 592 cases)
EXIT=0
（日志：.tao/logs/TESTCASES-008t-review2-validate_vectors.log）

$ make check
enabled components: none
references: 2
manifest validation: PASS
validate_vectors: 178/178 M1 identities covered OK (inventory sync OK; 15 data files, 592 cases)
repository checks: PASS
EXIT=0
（日志：.tao/logs/TESTCASES-008t-review2-make_check.log）

$ python3 tools/testcases/test_anti_forgery_008t.py
PASS: inject1: expected_fault=null [R2]
  -> ...reserved.yaml case[0]: reserved encoding must have expected_fault='UNDI', got None
PASS: inject2: expected_fault=ILLI [R2]
  -> ...reserved.yaml case[0]: reserved encoding must have expected_fault='UNDI', got 'ILLI'
PASS: inject3: reserved:true + word=M1 (0x10041000=ld.ub-rd) [R8/M1]
  -> ...reserved encoding word 0x10041000 matches defined encoding in opcodes.yaml (insn=ld.ub-rd, format=rrii, excluded_m1=False)
PASS: inject4: reserved:true + word=excluded_m1 (0x16000000=ld.t-rf) [R8/excluded]
  -> ...reserved encoding word 0x16000000 matches defined encoding in opcodes.yaml (insn=ld.t-rf, format=rrii, excluded_m1=True)
PASS: inject5: word=0x00000000 [R4]
  -> ...reserved encoding word must not be 0x00000000 (全零字 → illi → ILLI, §8.3)
PASS: valid reserved case

=== ALL 5 INJECTION + 1 VALID TEST PASSED ===
EXIT=0
（日志：.tao/logs/TESTCASES-008t-review2-anti_forgery.log）

$ git diff contracts/opcodes.yaml
（无输出 = 未修改）
```

#### 2. R-1 独立注入（reviewer 自建副本 `/tmp/opencode/TESTCASES-008t-review2/`，**不复用** engineer 脚本，patch `repo_dir` 后逐条注入）

（日志：`.tao/logs/TESTCASES-008t-review2-injections.log`）

```
A reserved:true + M1 word 0x10041000 (ld.ub-rd)         exit=1 CAPTURED
    ...case[0]: reserved encoding word 0x10041000 matches defined encoding in opcodes.yaml (insn=ld.ub-rd, format=rrii, excluded_m1=False); reserved only for QFC/子表 blank cells
B reserved:true + excluded_m1 word 0x16000000 (ld.t-rf) exit=1 CAPTURED
    ...case[0]: reserved encoding word 0x16000000 matches defined encoding in opcodes.yaml (insn=ld.t-rf, format=rrii, excluded_m1=True); ...
C word=0x00000000                                       exit=1 CAPTURED
    ...case[0]: reserved encoding word must not be 0x00000000 (全零字 → illi → ILLI, §8.3)
D1 expected_fault=null                                  exit=1 CAPTURED
D2 expected_fault=ILLI                                  exit=1 CAPTURED
E insn non-null (ld.ub-rd)                              exit=1 CAPTURED
baseline valid case                                     exit=0 OK
REAL reserved.yaml (2 cases)                            exit=0 OK
    validate_vectors: 178/178 M1 identities covered OK (inventory sync OK; 15 data files, 592 cases)
```

**R8 全覆盖独立验证**（防「只查 M1 子集」，日志 `.tao/logs/TESTCASES-008t-review2-r8-full.log`）：单文件构造 **256 条** case（`opcodes.yaml` 全部记录，`word := value`），一次运行：

```
total records: 256 excluded_m1: 78
exit: 1
R8 'matches defined encoding' error count: 256 (expect 256 )
RESULT: PASS
```

结论：R8 确实遍历 **全部 256 条**（含 78 条 `excluded_m1`），**非**只查 M1 子集，且真实 `reserved.yaml`（`0x08040001`/`0x1B040001`）零误报。

#### 3. `007t` 守卫独立验证（无回归，日志 `.tao/logs/TESTCASES-008t-review2-guards.log`）

```
baseline (real tree copy)         exit=0  validate_vectors: 178/178 ... 592 cases
(1) misc.yaml legality ILLI->null  exit=1 CAPTURED
    ...misc.yaml case[2]: legality case must have non-null expected_fault
(2) reserved case word missing     exit=1 CAPTURED
    ...reserved.yaml case[0]: encoding.word is required
```

结论：`007t` 遗留守卫（`legality` 非 null `expected_fault`）**真生效**；并因 `not is_reserved` 条件**不误报** reserved case（baseline exit 0）。「word 缺失」亦被捕获（R3.5/通用检查）。

#### 4. R-2~R-4 逐项核对

- **R-2**（反造假注入集）：脚本现含 `fault=null`、`fault=ILLI`、`reserved:true`+word 命中 M1、`reserved:true`+word 命中 `excluded_m1`、`word=0` 共 5 条，**覆盖验收标准 6 全清单**（且多出 excluded_m1 变体）；「5/5 + 1 valid」措辞与实现一致 ✅。**观察（非阻断）**：脚本相较第 1 轮去掉了「`insn` 非 null」与「`word` 缺失」两注入；二者 validator 仍能捕获（上述 §2.E / §3.(2) 独立验证 exit=1），且验收标准 6 未列此二项，故不构成返工。
- **R-3**（deferred.md）：line 45 标题改为「（`007t` 遗留，**已由 `008t` 消解**）」并在正文追加「✅ 已消解（`TESTCASES-008t`）…」✅。
- **R-4**（inventory.md）：「目标文件分布」表补 `| reserved.yaml | 0 |`（表头为「身份数」，总行=178 与门控一致）；`isa/` 15 文件 ↔ 表 15 行一致；另新增「保留编码 → UNDI」豁免段落明示**无身份、不参与门控** ✅。

#### 5. 约束核验（逐条）

| # | 验收标准 | 结果 | 证据 |
|---|---------|------|------|
| 1 | schema.md 写入保留编码规范（字段/约束/UNDI/与 ILLI 区分） | ✅ | `git diff` 含 `encoding.reserved` 定义、模板、字段约束表、ILLI 区分表 |
| 2 | validator 支持 reserved：豁免 7/8，强制 class/fault/status/word≠0，违规 exit 1 | ✅ | R1~R8 已实现；§2 注入 A~E 全 exit 1；R8 覆盖 256/256 |
| 3 | `reserved.yaml` ≥1 active UNDI，notes/spec_cite 给 QFC 位置 | ✅ | 2 case；QFC 位置独立核对（下表） |
| 4 | inventory.md 登记 UNDI 覆盖方式 | ✅ | 豁免段落 + 文件分布表 `reserved.yaml \| 0` |
| 5 | `contracts/opcodes.yaml` 未改 | ✅ | `git diff contracts/opcodes.yaml` 空 |
| 6 | 反造假：fault→null/ILLI、word 命中已定义、word=0 全部捕获 exit 1 | ✅ | 脚本 5/5 PASS；reviewer 独立注入 A~E 全 CAPTURED |
| 7 | validator 零错误；make check PASS | ✅ | 两条命令 EXIT=0（已重跑） |
| 8 | 不立 ADR | ✅ | 无新增 `adr-*.md`（`git status` 无 adr）；方案取舍记录在任务书 |
| 9 | 跨模块影响登记 deferred.md | ✅ | 新增 `encoding.reserved` 消费者同步义务（llvm/qemu/integ） |
| 10 | 未自行 commit | ✅ | `git log` 顶仍 `e02c584`；工作树仅未 staged 改动 |

**QFC 位置独立核对**（`spec/SimRISC-00` L245-262 主表，行=`op[7:3]`、列=`op[2:0]`）：
- `op=0x08`（`0000-1xxx` 行 + `x000` 列）：L248 该行**全空** → reserved ✅；程序化 `(0x08040001 & mask)==value` 命中 **0 条**，`op='0x08'` 不在 256 条中。
- `op=0x1B`（`0001-1xxx` 行 + `x011` 列）：L250 该行 `x000/x001/x010 = st.b/st.w/st.t`，`x011~x111` **空白** → reserved ✅；命中 **0 条**，`op='0x1B'` 不存在。
- 两 word 均 `!= 0`、`insn/mnemonic/format=null`、`expected_fault=UNDI`、`status=active`、`notes` 给 QFC 位置、`reserved.yaml` 无 generated header（手写）——与任务书模板/约束逐项一致。

**改动范围**：`git status --porcelain` 仅 `tests/vectors/{schema.md,inventory.md,isa/reserved.yaml}`、`tools/testcases/{validate_vectors.py,test_anti_forgery_008t.py}`、`.tao/knowledge/deferred.md`、本任务书 —— 全在 testcases 模块内，未触其它模块。
**既有断言未弱化**：`git diff tools/testcases/validate_vectors.py` 为纯新增（R1~R8 reserved 分支 + 1 条 `legality` 守卫；`load_opcodes`/`validate_file` 仅扩参/扩返回值），无删除既有断言；`validate_vectors` 592 cases 仍全过，`007t` 守卫独立复验生效。

#### 6. Finding 表

| ID | 级别 | 描述 | 状态 | 证据 |
|----|------|------|------|------|
| R-1 | 阻断 | reserved 无交叉校验可假通过 7/8 | **已消解** | R8；注入 A/B exit 1；256/256 全覆盖 |
| R-2 | 中 | 反造假注入集与标准 6 不符 | **已消解** | 5 条覆盖标准 6；脚本 PASS |
| R-3 | 低 | deferred.md 未标注 `007t` 守卫已消解 | **已消解** | deferred.md:45 已标注 |
| R-4 | 低 | inventory.md 文件分布表缺 `reserved.yaml` | **已消解** | 表行 `reserved.yaml \| 0` |
| N-1 | 观察（非阻断，超 008t 范围） | `contracts/opcodes.yaml` L5 将 `excluded_m1`（RF/cfx/LR-SC）标为 `decode: UNDI`，与 ADR-0004 D5.1「M1 排除但已定义的编码 → ILLI」及 contract-isa §9「UNDI=仅空白单元格」存在表述张力。**非 008t 引入**（该文件本次未改、内容为既有提交 a0fa10d），schema.md 区分表行取自 ADR-0004 D5.1 属忠实引用。留待架构师/`009t` 判定是否需在 opcodes.yaml 侧对齐。 | 登记 | opcodes.yaml L5；ADR-0004 L137/L325 |

#### 7. 判决与遗留

**Accepted**。第 1 轮 4 项 finding 全部消解并经独立重跑/独立注入证实；验收标准 1~10 全绿；无回归；未 commit。

**遗留/阻断项**：无阻断项。N-1 为超范围既有知识库表述张力，登记供架构师与 `009t` 定夺，不影响 008t 验收。

**采信项**（未独立重算，理由注明）：`reserved.yaml` 仅声明 `expected_fault=UNDI`，其**执行语义**由 harness/golden model 验证，超出 validator 能力（与 `005t` F4 同类）——本审查仅核对 schema/字段/位置依据（已独立核对）。
