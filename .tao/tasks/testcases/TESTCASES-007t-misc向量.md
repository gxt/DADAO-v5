# TESTCASES-007t: misc 向量（swym / illi / fence）

**模块**：testcases
**项目里程碑**：M1
**依赖**：`TESTCASES-006t`
**状态**：已验证

## 执行环境

**执行环境**：本地

## 接口规范

- 输入：
  - `tests/vectors/schema.md`（`TESTCASES-002t` 返工后：含 encoding 恒 fault 豁免）
  - `contracts/opcodes.yaml`（`swym-iiii`/`illi`/`fence` 编码字段；`op=value>>24`、`ha=(value>>18)&0x3f`）
  - `.tao/knowledge/contract-isa.md` §7（系统指令：`swym`/`fence`）、§8.2（保留编码 UNDI）、§8.3（全零字 → ILLI）、§9.1（ILLI）
- **输入说明**：`reg-*`（`003t`）、`mem-*`（`004t`）、`ctrl-*`（`005t`/`006t`）已生成并验证；本任务按 `schema.md` **从零生成** `misc.yaml`，**不是**重组/修复既有文件。
- 输出（**本任务拥有的文件**，`tests/vectors/isa/`）：
  - `misc.yaml`
- 约束：
  - 期望值**手工派生自 `contract-isa.md`/`spec/`/ADR-0004**，不得从 LLVM/QEMU 反推
  - 覆盖率主键 `(insn, format)`；M1 scope 以 `excluded_m1 != true` 为准（`swym`/`illi`/`fence` 均属 M1）
  - **只生成/修改 `misc.yaml`**；**不改** `contracts/`；**不改** `ctrl-*`/`reg-*`/`mem-*`
- **生成器**（依 `AGENTS.md`「生成器/脚本随产物保留」）：生成器须落在 **`tools/testcases/`**（可扩展已有生成器或新建），**不得**只放 `/tmp`；文件的 `# Generated by ...` header **必须指向真实存在的该生成器**（`006t` 曾因虚假 provenance 返工）
  - 参考仓库（`.dadao/DADAO-0628/`）的向量数据**仅内容溯源，不得作为执行依赖**（禁止复制数据正文）
  - 完成后不自行 commit

## 任务范围

### 1. 目标文件（从零生成）

> **数据来源说明**：`reg-*`/`mem-*`/`ctrl-*` 已由 `003t`~`006t` 生成；本任务按 `schema.md` 从零生成 `misc.yaml`；不再有「旧文件 → 新文件」的重组动作。

| 目标文件 | 覆盖身份 |
|---|---|
| `misc.yaml` | `swym-iiii`/`illi`/`fence`（3 个） |

- `br.*`/`jump`/`call`/`ret`（`005t`/`006t`）不在本任务。

### 2. F6：`illi` 的 encoding 豁免（复核）

- **事实**：`illi` 恒 ILLI（`contract-isa.md` §8.2/§9.1），`encoding` 类定义为「可解码执行无 fault」→ `illi` 的 encoding case **不可构造**，豁免。
- **要求**：`misc.yaml` 中 `illi` 的 encoding 列保持 `—`，覆盖率由 `legality` active（`expected_fault: ILLI`）满足；notes 说明豁免理由（恒 fault）。`schema.md` 的豁免条款由 `002t` 写入，本任务复核数据与之一致。

### 3. F10：`misc.yaml` 的 encoding 向量修复

- **要求**：逐条修正 `swym`/`fence` 的 encoding 向量（`illi` 豁免，见 §2），使 `word` 满足 `(word & mask) == value` 且可解码执行无 fault：
  - `swym`：占位指令（`contract-isa.md` §7），不改架构状态；`expected_state: null`、`expected_fault: null`、`status: active`；
  - `fence`：nop-like，不改寄存器；同上；
  - `expected_pc: null`（`swym`/`fence` 不改变 PC 目标——PC 顺延到下一条，属 harness 常规推进，不构成「改变 PC」；若 `002t` schema 对顺延有明确规定，按 schema 执行）。
- **全零字**：`0x00000000` 是 `illi` → **ILLI**，不是 UNDI（`§8.3`）；UNDI 的保留编码表达归 `TESTCASES-008t`，不在本任务。

### 4. 语义/合法性

- 复核 `illi` 的 legality（`expected_fault: ILLI`）、`swym`/`fence` 的 semantic（不改状态）。
- **`fence` 的 legality（`inventory.md` 标 `✓`，须补齐）**：`contracts/legality_rules.yaml` 明确 `fence` 的 `immu18_hi`/`immu18_mid`/`immu18_lo`（`bits[17:4]`）为 **SBZ** → **非零 SBZ → ILLI**。生成 1 条 `legality`（`expected_fault: ILLI`、notes 引 `legality_rules.yaml` 的 SBZ 规则名）。
- `swym-iiii` 的 legality 不适用（`inventory` 标 `—`，占位指令无 fault）。

## 验收标准

1. `misc.yaml` 从零生成，覆盖 `swym-iiii`/`illi`/`fence` 三个 M1 身份（无遗漏）
2. `illi` 无 encoding case，inventory 显式标注豁免理由（恒 fault）；覆盖率由 `legality` active 满足
3. `swym`/`fence` 的 encoding 经推演确认可解码执行无 fault（不 ILLI）；字段值 + 依据章节齐备
4. `fence` 的 legality 已生成（`bits[17:4]` 非零 SBZ → `ILLI`），notes 引 `legality_rules.yaml` 规则名
5. 未改 `contracts/`；未动 `ctrl-*`/`reg-*`/`mem-*`
6. `python3 tools/testcases/validate_vectors.py` 零错误；`make check` PASS
7. 生成器落在 `tools/testcases/`（`REPO` 由 `__file__` 推导、无硬编码绝对路径），重跑后 `misc.yaml` **byte-identical**；header 指向真实存在的生成器
8. 未自行 commit

## 背景（完整）

### 目标

把系统/杂项指令集中到 `misc.yaml`，复核 `illi` 的 encoding 豁免，并修复 `swym`/`fence` 的 encoding 向量（F10）。

### 设计理由

- `swym`/`illi`/`fence` 均属 M1 系统指令，但语义上互不相关且不共享格式族；集中在一个 `misc.yaml` 便于管理。
- `illi` 是唯一的恒 fault 指令，必须显式豁免并记录，避免覆盖率门控假阴性。

### 关键概念 / 数据

- **`swym`**：占位指令（`contract-isa.md` §7），不改变架构状态。
- **`fence`**：内存栅栏，M1 下 nop-like。
- **`illi`**：恒 ILLI（§9.1）；全零字 `0x00000000` = `illi 0`（§8.3）。
- **保留编码（QFC 空白格）→ UNDI**（§2.9/§8.2）：本任务不处理，归 `008t`。

### 上游引用

- DADAO-0628：`.dadao/DADAO-0628/code-agent/tasks/DL-001d-vector-data.md`、`DL-020a-encoding-vectors.md`（内容溯源，非执行依赖）

## 与 DADAO-0628 的差异（0.4.1 → 0.5.3）

1. **助记符**：`unimp`→`illi`；`swym`/`fence` 保留。
2. **`illi` 恒 ILLI**：与 0628 一致；v5 明确 encoding 豁免。
3. **UNDI 区分**：v5 明确「保留编码 → UNDI」与「全零字 → ILLI」不同（§8.2/§8.3）。
4. **覆盖率身份**：v5 用 `(insn, format)`。

## 已知坑 / 结论

1. **全零字不是 UNDI**：`0x00000000` → ILLI（§8.3）。
2. **`illi` encoding 不可达**：恒 fault，豁免；覆盖率由 legality 满足。
3. **`misc.yaml` 从零生成**：不依赖任何既有文件；`swym`/`illi`/`fence` 三个身份一次生成齐备。
4. **不自行 commit**：完成后等待审查。

## 参考

- DADAO-0628：`.dadao/DADAO-0628/code-agent/tasks/DL-001d-vector-data.md`、`DL-020a-encoding-vectors.md`（内容溯源）
- 本项目：`.tao/knowledge/contract-isa.md` §7/§8/§9、`contracts/opcodes.yaml`、`tests/vectors/schema.md`
- 知识库：`.tao/knowledge/MEMORY.md`、`.tao/knowledge/deferred.md`

## 完成区

**测试结果**：通过 6/6（`validate_vectors.py` exit=0，`make check` PASS，178/178 M1 identities covered）
**修改文件**：
- `tests/vectors/isa/misc.yaml`（新建，6 cases：swym-iiii encoding+semantic / illi legality / fence encoding+legality+semantic）
- `tools/testcases/generate_misc.py`（新建生成器）

**验收结果**：
- `python3 tools/testcases/validate_vectors.py` → `178/178 M1 identities covered OK (inventory sync OK; 14 data files, 590 cases)`
- `make check` → `repository checks: PASS`
- 独立全量重算 6 条/6 checks，**0 mismatch**
- 生成器 md5 确定性：两次运行 md5=7e2b7a94c0fce9e699939c06f8c26fe9，byte-identical
- 反造假：swym encoding word 错误 → validator exit=1 捕获 ✓

**新发现/坑**：
- `swym` 的 encoding word=0x77000000 与 mask=0xFF000000/value=0x77000000 匹配，需注意 op 字节是 0x77 不是 0x70（QFC 主表 line 233 确认）
- `fence` 的 SBZ 域是 immu18 的 bits[17:4]（即 fields immu18_hi+immu18_mid+immu18_lo 的高位），不是整个 immu18；低 4 位编码屏障类型可非零
- `illi` 的 word=0x00000000 同时满足 opcodes.yaml 的 mask/value（mask=0xFFFC0000, value=0x00000000），且是 §8.3 全零字（触发 ILLI，非 UNDI）
- validator 对 `expected_fault` 的校验仅限 enum 值合法（null/ILLI/UNDI/...），不校验 legality 类必须非 null；semantic 级 fault 正确性靠独立重算兜底
- `fence` 的 opcodes.yaml legality 规则 `immu18_lo[5:4] == 0` 是更严格的约束（bits[5:4] 也 SBZ），但 `legality_rules.yaml` 的 `sbz_nonzero` 规则覆盖 bits[17:4]；本任务 legality case 用 immu18_hi≠0 触发，足够

**遗留问题**：无

## 审阅记录

### 第 1 轮 engineer 自审

**审查范围**：`tools/testcases/generate_misc.py`（全文）、`tests/vectors/isa/misc.yaml`（6 cases 全文）、`contracts/opcodes.yaml`（swym/illi/fence 记录）、`contract-isa.md` §7/§8/§9、`contracts/legality_rules.yaml`（sbz_nonzero）、`tests/vectors/schema.md`、`tests/vectors/inventory.md`（3 行 M1 记录）。

**逐行审查发现**：

| # | finding | 严重度 | 判决 |
|---|---------|--------|------|
| 1 | `illi` legality case 的 encoding word=0x00000000 同时是 §8.3 全零字 | 正确 | ✅ 无问题：notes 已说明 |
| 2 | `fence` legality case 的 word=0x00050000 的 SBZ non-zero 来自 immu18_hi=0x01 | 正确 | ✅ 验证：(0x00050000 & 0xFFFC0000)==0x00040000 ✓; bits[17:12]=1≠0 ✓ |
| 3 | `swym` encoding word=0x77000000 的 op 字节 | 正确 | ✅ QFC 主表 line 233: swym at op=0111-0111=0x77 ✓ |
| 4 | `fence` semantic expected_state={} 但 fence 是内存屏障 | 正确 | ✅ §7.3: M1 下 nop-like，不改寄存器/内存；schema.md: PC 顺延不算改变 PC |
| 5 | `illi` 无 encoding/semantic case | 正确 | ✅ F6: 恒 ILLI，豁免；inventory line 18 标 `—`/`—`/`✓` |
| 6 | `swym` 无 legality case | 正确 | ✅ 占位指令无 fault；inventory line 195 标 `—` |
| 7 | 生成器 REPO 由 `__file__` 推导 | 正确 | ✅ 无硬编码绝对路径 |

**处置表**：

| finding | 处置 | 改了什么 | 复验证据 |
|---------|------|---------|---------|
| 1-7 | ✅无问题 | — | 独立全量重算 6 条 0 mismatch + validator exit 0 |

**判决**：所有 finding 均为「正确确认」，无阻断项。标 `待验收`。

### 第 1 轮 reviewer 验收

**审查范围**：`tests/vectors/isa/misc.yaml`（6 cases 全文）、`tools/testcases/generate_misc.py`（全文）、`tools/testcases/validate_vectors.py`、`tests/vectors/schema.md` F6 条款、`tests/vectors/inventory.md`（`illi`/`fence`/`swym-iiii` 三行）、`contracts/opcodes.yaml`（`swym-iiii`/`illi`/`fence` 三条）、`contracts/legality_rules.yaml`（`sbz_nonzero`）、`contract-isa.md` §7.1/§7.2/§7.3/§8.3/§9.1 与附录 A.1/A.6（`op=0x77`→`swym`；MISC-AMO `000-000`→`illi`、`000-001`→`fence`）。所有判决基于**本会话自行执行的命令**，未采信完成区叙述。

#### 1. 重跑记录（真实输出 / 退出码）

**① `python3 tools/testcases/validate_vectors.py`**（log `.tao/logs/TESTCASES-007t-review-validate.log`）
```
validate_vectors: 178/178 M1 identities covered OK (inventory sync OK; 14 data files, 590 cases)
EXIT=0
```

**② `make check`**（log `.tao/logs/TESTCASES-007t-review-make-check.log`）
```
enabled components: none
references: 2
manifest validation: PASS
validate_vectors: 178/178 M1 identities covered OK (inventory sync OK; 14 data files, 590 cases)
repository checks: PASS
EXIT=0
```

**③ 独立全量重算**（自写 oracle，`/tmp/opencode/TESTCASES-007t-review/recompute_misc.py`，独立读取 `opcodes.yaml` + `legality_rules.yaml`，**不 import 生成器/validator**；log `.tao/logs/TESTCASES-007t-review-recompute.log`）
```
OK  case[0] swym-iiii/iiii/encoding word=0x77000000: fault=None decode_ok=True
OK  case[1] swym-iiii/iiii/semantic word=0x77000000: fault=None decode_ok=True
OK  case[2] illi/oiii/legality       word=0x00000000: fault='ILLI' decode_ok=True
OK  case[3] fence/oiii/encoding      word=0x00040000: fault=None decode_ok=True
OK  case[4] fence/oiii/legality      word=0x00050000: fault='ILLI' decode_ok=True
OK  case[5] fence/oiii/semantic      word=0x00040000: fault=None decode_ok=True
TOTAL=6 MISMATCH=0
EXIT=0
```
逐条重算内容：`(word & mask)==value` 可解码性、fault 判定（`illi` 恒 ILLI；`fence` SBZ `bits[17:4]` 非零→ILLI；`swym` 无 fault）、class↔state/fault 一致性、status。**6 条 / 0 mismatch**。

**④ 生成器确定性**（在副本 `/tmp/opencode/TESTCASES-007t-review/base` 连跑 3 次）
```
repo misc.yaml : 7e2b7a94c0fce9e699939c06f8c26fe9
run1           : 7e2b7a94c0fce9e699939c06f8c26fe9
run2           : 7e2b7a94c0fce9e699939c06f8c26fe9
run3           : 7e2b7a94c0fce9e699939c06f8c26fe9
```
三次运行与仓库内文件 **byte-identical**（md5 一致）。

#### 2. 独立验证重点逐条核对

| # | 验证点 | 核验方式（自跑） | 结论 |
|---|--------|------------------|------|
| 1 | **F6**：`illi` 无 encoding、无 semantic，覆盖率由 legality 满足 | 读 yaml：`illi` 仅 1 条 case，`class: legality`、`expected_fault: ILLI`；与 inventory L18 `encoding — / legality ✓ / semantic —` 一致 | ✅ |
| 2 | **F10**：`swym`/`fence` encoding `(word & mask)==value`、无 fault | oracle：`swym` word=0x77000000、`fence` word=0x00040000 → `decode_ok=True`、`fault=None`；两 case `expected_pc:null`/`expected_fault:null`/`status:active` | ✅ |
| 3 | **`fence` legality** SBZ 规则、notes 引规则名 | word=0x00050000：`(w&0xFFFC0000)==0x00040000`；`ha=1`、`immu18_hi=0x01`，`bits[17:4]=0x1000≠0` → ILLI。notes 含 `legality_rules.yaml rule=sbz_nonzero`、引 §7.3 | ✅ |
| 4 | **全量重算**（不抽样） | 见上 ③：6/6、0 mismatch | ✅ |
| 5 | **生成器**：`REPO` 由 `__file__` 推导、无硬编码绝对路径、重跑 byte-identical、header 指向真实生成器 | `REPO = dirname(dirname(dirname(abspath(__file__))))`（L29）；`grep` 无 `/mnt`/`/home`/绝对路径；header `# Generated by tools/testcases/generate_misc.py`，该文件实际存在；3 次运行 md5 一致 | ✅ |
| 6 | **反造假** | 见下 §3 | ✅（覆盖缺口已定位，非本任务缺陷） |

副本路径基线核验（src 事实）：`contract-isa.md` L1038 `0x77→swym(iiii)`、L1171/1172 `000-000→illi(oiii)`/`000-001→fence(oiii)`；§7.3 L869 `bits[17:4] SBZ 非零行为保留`；§8.3 全零字→ILLI。

#### 3. 反造假注入（副本，raw 输出见 `.tao/logs/TESTCASES-007t-review-antifraud.log`）

| 注入 | validator 结果 | 独立 oracle | 判定 |
|------|----------------|-------------|------|
| baseline | exit=0（178/178, 590 cases） | exit=0 | 基线一致 |
| **A** `illi` legality `expected_fault→null` | **exit=0（未捕获）** | exit=1 `MISMATCH case[2] ... derived='ILLI' yaml=None` | 只能靠重算 |
| **B** 给 `illi` 增一条 encoding case | **exit=0（未捕获）** | exit=1 `MISMATCH case[6] illi/oiii/encoding ... derived='ILLI' yaml=None` | F6 未被 validator 强制 |
| **C** `fence` legality SBZ 清零仍标 ILLI | **exit=0（未捕获）** | exit=1 `MISMATCH case[4] ... derived=None yaml='ILLI'` | 只能靠重算 |
| **D** `swym` encoding word=0x70000000 | exit=1（捕获，mask/value 不符） | exit=1 | 被捕获 |
| **E** `fence` encoding 置 SBZ 非零仍标 fault=null | **exit=0（未捕获）** | exit=1 `MISMATCH case[3] ... derived='ILLI' yaml=None` | F10「无 fault」未被 validator 强制 |

**validator 覆盖缺口（engineer 已完成区已披露，属实）**：
- 缺口 1（check 14 第二项）：`legality` 类只校验 `expected_state==null`，**不校验 `expected_fault` 非 null** → 注入 A 漏过。
- 缺口 2（无 F6 规则）：validator **不检查 `illi` 是否缺 encoding case**，也不检查 encoding case 的 fault 语义 → 注入 B/E 漏过。
- 缺口 3：SBZ 非零（`sbz_nonzero`）、`illi` 恒 ILLI 等**语义级 fault 判定**不在 validator 能力内（`schema.md` L178 已声明「完整语义期望值重算属 golden model 模块」）→ 注入 C 漏过。

结论：上述缺口是 validator 的**已知能力边界**，`schema.md` 明文将其划归重算/golden model，**非本任务交付缺陷**；且有**独立全量重算兜底**（6/6、0 mismatch），本任务数据在这些缺口维度上均正确。

#### 4. 约束核验（逐条）

| 约束 | 核验命令/证据 | 结论 |
|------|---------------|------|
| 1. 覆盖 3 个 M1 身份 | oracle `identities=[fence/oiii, illi/oiii, swym-iiii/iiii]`；validator 178/178 | ✅ |
| 2. `illi` 无 encoding、豁免理由在 inventory | yaml + inventory L18/L199-201 | ✅ |
| 3. `swym`/`fence` encoding 无 fault、字段/依据齐备 | oracle 逐条 + spec_cite 非空 | ✅ |
| 4. `fence` legality 引规则名 | notes 含 `rule=sbz_nonzero`、§7.3 | ✅ |
| 5. 未改 `contracts/` | `git status` contracts/ 无改动；`git diff -- contracts/` 空 | ✅ |
| 6. 未动 `ctrl-*`/`reg-*`/`mem-*` | `git status --short` 仅 `misc.yaml`（新）+ `generate_misc.py`（新）为未跟踪；其余 13 个 isa/*.yaml 均无 M | ✅ |
| 7. 生成器在 `tools/testcases/`、`REPO` 由 `__file__`、byte-identical、header 真实 | §2 第 5 行 | ✅ |
| 8. 未 commit | `git log -1` = `9bd625b`(006t)；misc.yaml/生成器仍为 `??` 未跟踪 | ✅ |
| 9. validator exit=0 + `make check` PASS | §1 ①② | ✅ |

#### 5. 判决

**Accepted**。

- 验收命令块在本会话**自跑**下全部通过（validator exit=0、`make check` PASS）。
- 独立全量重算 **6 条 / 0 mismatch**；F6/F10/`fence` legality 三点均独立复算无误。
- 8 条硬约束逐条守住（`contracts/` 与既有向量未动、未 commit）。
- 反造假矩阵说明：validator 存在 3 处**已披露的能力边界**（legality fault 非 null、F6/F10 语义），但均由独立重算覆盖；本任务数据无错值，**不构成返工项**。

**遗留/阻断项**：无阻断。建议（非本任务返工）：validator 可后续任务考虑补「`legality` 类 `expected_fault` 必须非 null」的机械守卫（注入 A 属可机械捕获项），登记供 `009t` 审计参考。

**本轮修改的文件**：仅追加本审阅记录；临时注入副本与 oracle 位于 `/tmp/opencode/TESTCASES-007t-review/`（已保留原文件，未污染仓库）；未改完成区；未 commit。
