# Changelog

任务对仓库代码的实质改动记录（由 `/complete` 追加）。

| 日期 | 描述 | 执行方 |
| --- | --- | --- |
| 2026-09-23 | **组件补丁集重整 + 规范落地**。①新增 **`docs/spec/component-patching.md`**（v5 规范层首篇）：树形补丁集（`patches/<上游相对路径>.patch`）+ **一文件一补丁**（双向唯一）+ **新增文件补丁必须终态** + `git apply`（弃 `git am`）+ 4 条断言；②`ADR-0002 D4` **就地修订**（rev. 2026-09-23，属决策反转，偏离已记录）；③脚本：`make_patch.py` 重写（逐路径裸 `git diff` 导出）、`apply_series.py` 重写（`git apply --check` + `apply` + **幂等检测**）、新增 `check_patch_tree.py`（4 断言）并接入 `make check`（`check-patch-tree`）；④**M1 补丁集重整**：LLVM 8→**36**（新增 32/修改 4）、QEMU 8→**31**（新增 25/修改 6），**安全网**：重整后 tree hash 逐组件与重整前**完全一致**（`llvm-project` `0e058573…`、`qemu` `4a14d56c…`）；⑤组件新增 `README.md`/`changelog.md`；⑥`deferred.md` 两条消解（`make prepare` 非幂等、源树提交作者不实）。 | 主会话 + 用户 |
| 2026-09-22 | **M1 达成（用户确认）**。`INTEG-004m` 置里程碑 ⇒ 6/6 模块 M1 完成（`SPEC-011m`/`TESTCASES-012m`/`INFRA-014m`/`LLVM-015m`/`QEMU-021m`/`INTEG-004m`），`milestones.md` M1 = **达成**。核验（真实输出）：`llvm-lit tests/lit/E2E/` **3/3**；`tools/integ/check_interface_alignment.py` **80/80/0 EXIT 0**；`make check` **EXIT 0**。本会话另完成：`INTEG-001k`~`003t`（E2E 冒烟 + 接口对齐）、`LLVM-013t`（`wpN` 静默误编码修复，补丁 `0007`）、`LLVM-014t`（ELF `e_flags` 修复，补丁 `0008`）；里程碑顺延 `LLVM-013m→014m→015m`；`fence` 保持 deferred（`ISS-056` `blocks: []`）。 | 主会话 + 用户 |
| 2026-09-12 | INFRA-002t：建立仓库骨架（`.gitignore`、`components/`/`scripts/`/`containers/` 骨架、`docs/repository-layout.md`） | engineer |
| 2026-09-12 | INFRA-003t：manifest 系统（`manifests/components.lock.toml`、`manifests/references.lock.toml`、`scripts/manifest_check.py`） | engineer |
| 2026-09-12 | INFRA-004t：组件获取与打补丁工具（`scripts/fetch.py`、`apply_series.py`、`make_patch.py`、`fetch_refs.py`；`.cache/` 持久 bare mirror + `.work/` 可再生工作树） | engineer |
| 2026-09-12 | INFRA-005t：环境与状态工具（`scripts/doctor.py`、`status.py`、`clean_work.py`；clean_work 保护 `.cache/`） | engineer |
| 2026-09-12 | INFRA-006t：顶层 `Makefile` 编排（help/manifest-check/doctor/status/fetch/apply-series/prepare/build-*/docker-*/check；build stub 不假装成功） | engineer |
| 2026-09-12 | INFRA-007t：开发容器 `containers/dev/Dockerfile`（ubuntu:24.04 + LLVM/QEMU 依赖 + clang，M1 scope 不含 gem5；容器内 `make doctor` 报 native） | engineer |
| 2026-09-12 | INFRA-008t：补 v5 自身 ADR（`adr-0001-greenfield-rebuild.md`、`adr-0002-build-orchestration.md`，含 `.cache/` mirror 决策；改 5 文件引用） | engineer |
| 2026-09-12 | SPEC-002t：ISA 规范合约按 M1 范围重新生成（`contract-isa.md`；**RA 进 M1**，浮点 RF/特权/原子 Excluded） | engineer |
| 2026-09-12 | SPEC-003t：编码表按 M1 范围重生成（`contracts/opcodes.yaml`：M1 178 + `excluded_m1` 78；修正旧版 2 处 ha 错误 + 5 条块赋值 legality 字段） | engineer |
| 2026-09-12 | SPEC-004t：ABI 合约收窄到 M1 最小事实（`contract-abi.md` + `contracts/abi.yaml`；完整调用约定 `Deferred to M2`） | engineer |
| 2026-09-12 | SPEC-005t：Object ABI ADR-0003（M1：ELF 头字段 + 段/流水线；重定位 `Deferred to M2`） | engineer |
| 2026-09-13 | SPEC-005t 修订：ADR-0003 `e_flags` 由 1 位标志改为 bits0–7 版本字段（M1=1，bits8–31 保留） | engineer |
| 2026-09-13 | SPEC-006t：Test Machine ADR-0004（内存映射/复位值/exit port/异常可观测，D1–D6 冻结；Status=Candidate） | engineer |
| 2026-09-13 | ADR-0004 重判修订：D1 改核内地址空间模型（ROM `0xffff_ffff_0000`/RAM `0xffff_0000_0000` 16MiB/Exit `0xffff_8000_0000`）、D5 fault 码改 spec cause 派生（ILLI `0x88`…IALIGN `0x8D`，unmapped `0x87`）、D3 harness 超时兜底、D6 示例修正；同步下游任务书 QEMU-003t/QEMU-015t/INTEG-002t/TESTCASES-006t/008t | architect |
| 2026-09-13 | SPEC-006t 返工复验：ADR-0004 修订后重开验收（更新验收脚本至 109/109 PASS；reviewer+architect 双模型 Accepted；ADR 修正 D6.4 距离与状态说明两处），任务置 `已验证` | architect |
| 2026-09-14 | ADR-0003 rev. 2026-09-14：D2 登记补充（绝对地址并入「绝对 64-bit 数据地址」、不单列 wyde 地址构造场景；相对分支/call/jump `<<2`、有效范围=位宽+2；`rela.si` `<<12`、4KB 对齐、与页无关、无 `<<2`） | architect |
| 2026-09-14 | SPEC-007t：ELF 合约（`.tao/knowledge/contract-elf.md`；§1 头字段 + §5 段对齐/VA=PA + §6 pipeline；§2/§3/§4 重定位 `Deferred to M2`） | engineer |
| 2026-09-14 | 模块重划：`verif` 解散——通用 CI 检查→`infra`（`INFRA-010t` issue registry/`INFRA-011t` spec 引用审计/`INFRA-012t` drift）、验证依据→`spec`（`SPEC-008t` legality/`SPEC-009t` QFC）、组件自测→`qemu`（`QEMU-014t`~`019t` harness×5+trans lint）/`llvm`（`LLVM-012t` lit oracle）、集成→新模块 `integ`（`INTEG-002t` E2E/`INTEG-003t` 接口对齐）；目录 `verif/`→`contracts/`（数据）+`tools/<module>/`（脚本）、`scripts/`→`tools/infra/`；里程碑顺延（`INFRA-013m`（现 `INFRA-014m`）/`SPEC-011m`/`LLVM-015m`/`QEMU-021m`/`INTEG-004m`） | architect |
| 2026-09-14 | SPEC-008t 重做+返工：`contracts/legality_rules.yaml` 重新生成（增补 RA 规则；删除 `ra0_no_exception`——ra0 可读写无异常；fault 枚举限定 6 值 `ILLI/UNDI/MALIGN/IALIGN/RASOF/RASUF`；对齐 `legality` 字段引用；修正 `spec_cite` 重复键/行号与 RF/LR/CFX 规则状态） | engineer |
| 2026-09-14 | SPEC-009t：QFC 覆盖校验（`tools/spec/check_qfc_coverage.py`；QFC 表 ↔ `contracts/opcodes.yaml` 双向比对，256/256 差异 0，只读 informational） | engineer |
| 2026-09-14 | SPEC-010t：Spec 冻结（`docs/impact-matrix.md` 逐节覆盖 + `README.md` 冻结状态标 `已冻结`）；`contract-elf.md` Status → Accepted | engineer |
| 2026-09-14 | SPEC-011m：M1 spec 里程碑达成（`002t`~`010t` 已验证，合约/编码表/ADR/impact-matrix 齐备） | architect |
| 2026-09-15 | TESTCASES 任务集二次重排：按运算族/load-store/控制转移/jump-call-ret/misc 分解为 `002t`~`009t`+`010m`；文件布局方案 A（14 个 `isa/*.yaml`）；F1→`003t`、F10 分摊、F5→`008t`、F7 采用 `expected_pc` 方案全 active；旧 `004t`/`005t`/`006t` 已达成关闭（证据见 `001k`） | architect |
| 2026-09-15 | TESTCASES-002t 返工（从零重建向量基础设施）：`schema.md` 新增可选 `expected_pc` + encoding 类对恒 fault 指令（`illi`）豁免；`inventory.md` 增 `format` 列（178 行唯一可辨）+ 机械同步；`validate_vectors.py` 补 R2/R4/F9②③④；覆盖率门控改为**声明级**（数据级归 `009t`） | engineer |
| 2026-09-16 | TESTCASES-003t：寄存器族 ISA 向量从零生成（`reg-arith`/`reg-logic`/`reg-shift-extend`/`reg-compare`/`reg-cond-assign`/`reg-imm-block`，130 身份/366 条）；含 **F1**（`orrr` shamt 取寄存器形式）、**F10**（encoding 可执行无 fault）、`rela.si` 改 active；validator 追加 F9① + class↔fault/state 守卫；生成器提交至 `tools/testcases/generate_isa_vectors.py` | engineer |
| 2026-09-16 | TESTCASES-003t 两轮返工：R1 定宽有符号运算补符号扩展（`.s*` 符号 / `.u*` 零扩展）；R2 16 条 `div`/`rem` encoding 预置非零除数；R3 `rela.si` notes 补 PC 来源；R4 `rem.s*` 改 truncate-toward-zero 余数。第 2 轮 reviewer 独立全量重算 366 条 **0 mismatch**，判决 Accepted（architect 交叉复核确认） | engineer |
| 2026-09-16 | 治理规则（用户裁定）：`AGENTS.md`「临时目录」增「生成器/脚本随产物保留」（判据=产物是否入库；"一次性"≠可丢弃）；「中间验证规范」增「数据/期望值类任务附加要求」（size/sign 敏感用例独立全量重算、不得以 validator 绿灯为唯一判据、错值当场修） | architect |
| 2026-09-16 | TESTCASES-004t：访存向量从零生成（`mem-rd`/`mem-rb`/`mem-ra`，30 身份/172 条）；**F10** 访存 encoding 一律可解码执行无 fault（base=`rb3` 未使用寄存器 + 预置 RAM 基址、dest/源非 rd0、`immu6≥1`）；validator 追加 F10 守卫；生成器提交至 `tools/testcases/generate_mem_vectors.py` | engineer |
| 2026-09-16 | TESTCASES-004t 返工：F-1 rrii 偏移编入 `imms12`（原误用无作用的 `rd2`，15 条 semantic EA≠memory）；F-2 rrii MALIGN 令 EA 真未对齐（12 条）；F-3 删 2 条 `base=rb0` 无效 case；F-5/F-6 inventory `boundary` 列与 fault `spec_cite` 补正。第 2 轮 reviewer 独立全量重算 172 条 **0 mismatch**，判决 Accepted（architect 交叉复核确认） | engineer |
| 2026-09-16 | TESTCASES-005t：`ctrl-br.yaml` 从零生成（10 个 `br.*` 身份/30 条）；**F7** 每身份 taken+not-taken 全测（`expected_pc`：taken=`rb0+8`（`imm=2`）、not-taken=`rb0+4`，`rb0=0xffff00000000`）；F10 encoding 无 fault 不自跳；validator 追加 F7 `expected_pc` 存在性规则（作用域 `br.*`，供 `006t` 扩展）；生成器 `tools/testcases/generate_ctrl_br.py` | engineer |
| 2026-09-16 | TESTCASES-005t 返工：F1 10 条 encoding 的 `imm` 由 `0` 改 `2`（原 5 条条件默认为真 → 确定性自跳）；F3 生成器统计口径修正。第 2 轮 reviewer 独立全量重算 20 条 semantic **0 mismatch**、10/10 encoding 不自跳，判决 Accepted（architect 交叉复核确认） | engineer |
| 2026-09-16 | TESTCASES-006t：`ctrl-jump`/`ctrl-call`/`ctrl-ret` 从零生成（5 身份/16 条）；**F7** `jump`/`call` 生成即 active（`expected_pc`：`imm=2` → `rb0+8`）；`call` 压栈用 `expected_state.ra`（`ra63` 高16 计数 + 低48 `PC+4`）；`ret` 弹栈；validator 的 F7 规则**就地扩展**至 `jump`/`call`/`ret`；生成器 `tools/testcases/generate_ctrl_jump_call_ret.py` | engineer |
| 2026-09-16 | TESTCASES-006t 返工：G-1 补生成器（消除虚假 provenance，md5 确定性一致）；G-2 inventory 措辞；G-3 补 `call` §5.6.1 case 2/3 semantic；G-4 记录 harness 依赖。第 2 轮 reviewer 独立全量重算 16 cases/39 checks **0 mismatch**，判决 Accepted（architect 交叉复核确认） | engineer |
| 2026-09-16 | TESTCASES-007t：`misc.yaml` 从零生成（3 身份/6 条：`swym-iiii`/`illi`/`fence`）；**F6** `illi` 恒 ILLI → encoding/semantic 豁免、覆盖率由 `legality` 满足；F10 `swym`/`fence` encoding 无 fault；`fence` 的 SBZ（`bits[17:4]` 非零）→ ILLI（legality）；生成器 `tools/testcases/generate_misc.py` | engineer |
| 2026-09-16 | TESTCASES-008t：F5 落地「保留编码 → UNDI」向量层表达（方案 A：复用 `class: legality` + `encoding.reserved: true`，不立 ADR）；`schema.md` 增规范；`validate_vectors.py` 加 reserved 分支 + **R8 交叉校验**（word 不得匹配 `opcodes.yaml` 任何记录，含 `excluded_m1`）；新建 `tests/vectors/isa/reserved.yaml`（2 条 UNDI）；`inventory.md` 登记；反造假脚本 `tools/testcases/test_anti_forgery_008t.py`；顺带消解 `007t` 遗留的 `legality.expected_fault` 非 null 守卫 | engineer |
| 2026-09-16 | TESTCASES-009t：ISA 向量全量再审计（15 文件/597 条逐族独立重推导，**0 残留错值**）；新增审计记录 `docs/testcases-009t-audit.md` 与脚本 `tools/testcases/009t-audit.py`（318/320, 0 mismatch）；`validate_vectors.py` 追加**数据级覆盖率门控**（按 inventory 每行 ✓ 类逐 `(insn,format)` 校验 active case，**只报不拦**，用户裁定 B）；`reg-cond-assign.yaml` +5 条 C-27 deferred overlap；**发现 154 数据级缺口**（141 legality+2 boundary+11 overlap）并登记 `deferred.md` | engineer |
| 2026-09-16 | TESTCASES-009t 经 6 轮 reviewer 验收收敛（5 次打回全在审计脚本/记录：`.sb`/`rwii`/`rb` 分支/`rd2ra`/计数口径/`cmp` 符号），**数据自第 1 轮即 0 mismatch**；architect 交叉复核确认 Accepted；登记「工具交付物 vs 数据交付物分层验收」教训；建议新建 `010t` 补数据 + 里程碑顺延 `011m` | architect |
| 2026-09-16 | LLVM 模块计划级复核 + reviewer S1–S13 修订落盘：S1 分支偏移公式去 +4（`contract-isa.md` §5.2–5.4 `Addr=rb0+(imm<<2)`，无流水线偏移）+ fixup kind 改自定义 `DADAO_FK_PCRel_2`；S2 格式数 12→9（`crrr`/`crii`/`ciii` Excluded from M1）+ §2.2→§2.3；S3/S13 `LLVM-010t` 改名「系统指令助记符与 smoke 修正」+ 重写（`escape`/`trap` Excluded，M1 = `swym`/`illi`/`fence`）；S4 补 `SPEC-003t` 依赖到 `006t`/`007t`/`008t`/`009t`/`010t` + 回写 `001k` 任务表；S5 `011t` 同名助记符消歧方案（方案 A：`insn` 全名作 AsmName）+ 补 `LLVM-005t` 依赖；S6 `012t` 依赖补 `LLVM-011t`；S7 `012t` oracle 增强（mnemonic ↔ 命中记录 + N 下限）；S8 `deferred.md` 修 `LLVM-012t` 陈旧引用；S9 DecoderMethod 归属澄清（`.td` 注解在 `005t`，C++ 实现在 `008t`）；S10 `002t` LLVM_SRC 已知坑标注已解决；S11 `013m` 核验清单补全；`LLVM-001k` 状态置 `已验证` | architect |
| 2026-09-17 | 新增规则「大文件下载与镜像」（用户裁定）：`AGENTS.md` 增该节 + 知识库 `.tao/knowledge/mirrors.md`（执行细则）。要点：从国外站下载较大软件（LLVM/QEMU/linux-kernel 等）**先查教育网联合镜像站** <https://mirrors.cernet.edu.cn/list/> → 逐个测连通性/速度 → 给建议（含直连对照）→ **用户确定**；有明确 release/tag 时**默认浅下载但仍先问** | architect |
| 2026-09-17 | 组件锁多源规划：新任务 INFRA-009t（`components.lock.toml` 新增 `[[component.source]]`、`manifest_check.py` source 校验、`fetch.py` 多源选源）、ADR-0005（组件锁多源 schema 与选源规则）、LLVM 基线 ADR 顺延为 ADR-0006（`adr-0005-llvm-baseline.md` → `adr-0006-llvm-baseline.md`，4 文件 13 处引用同步） | architect |
| 2026-09-17 | INFRA-009t：组件锁多源落地（`manifests/components.lock.toml` llvm 条目新增 `[[component.source]]`=SJTU；`tools/infra/manifest_check.py` 新增 `repository` 非空 + `source` 的 name 唯一/url 非空校验；`tools/infra/fetch.py` 新增 `select_source()` 实现 ADR-0005 D2 选源并打印实际源，`sync_mirror` 短路保留）。reviewer Accepted（独立重跑 manifest-check/make check exit 0、选源 5 例、manifest_check 反例 10 组、真实 mirror 短路离线验证）+ architect 交叉复核确认 Accepted（跨模块排查 `fetch_refs.py`/`apply_series.py`/`status.py`/`Makefile`/README 均不需改）；完成区 2 处可审计性问题当场修正 | engineer |
| 2026-09-17 | LLVM 基线浅镜像获取（SJTU，用户选定）：`git clone --bare --depth 1 --branch llvmorg-23.1.1` → `.cache/llvm.git`（43.7 s，376.53 MiB，189,410 objects，180,593 文件，非 treeless）；`llvmorg-23.1.1` tag 对象 `e7ce3600…` → **commit `6dfe1677ab8dffbc6ec13d53a1e0215d75147689`**；实测记录见 `.tao/logs/LLVM-002t-mirror.log`。未改仓库被跟踪文件 | engineer |
| 2026-09-17 | 按原始仓库名命名规划：新任务 `INFRA-013t`（组件 `name`→`llvm-project`、参考 `id`→`DADAO-0628`/`DADAO`、manifest/Makefile/缓存/文档路径同步）；里程碑 `INFRA-013m`→`INFRA-014m` 顺延（含 milestones.md/INFRA-001k 任务表/历史记录加注） | architect |
| 2026-09-17 | `INFRA-013t`：标识符按原始仓库名落地——`manifests/components.lock.toml` `name`→`llvm-project` + `patch_series`→`components/llvm-project/patches/series`；`manifests/references.lock.toml` `id`→`DADAO-0628`/`DADAO`；`Makefile` `LLVM_SRC`→`.work/source/llvm-project/llvm` + `component-enabled,llvm-project` + 报错文案；缓存迁移 `.cache/llvm-project.git`/`.cache/refs/DADAO-0628.git`/`.cache/refs/DADAO.git` + 两工作树 origin 修正；15 个任务书路径/标识符同步（0628 自身路径不动）。reviewer 第 1 轮 Needs Revision（B1 `LLVM-003t:21` 漏改 + N1 越界缩进 + N2/N3 完成区口径/归属 + N4 三处陈旧示例）→ 返工 → 第 2 轮 **Accepted**（9/9）+ architect 交叉复核确认（新发现 F1 `INFRA-009t:258` 陈旧路径，与 P1/P2 由主会话修正） | engineer |
| 2026-09-17 | 新增 ADR-0007（DADAO 注册通道 = 加入 `LLVM_ALL_TARGETS`）+ `LLVM-003t` 任务书同步（交付物/已知坑/参考）+ `LLVM-002t` 完成区补记 | architect |
| 2026-09-17 | `LLVM-002t`：LLVM 组件基线落地——ADR-0006 重写并置 **Accepted**（D1 版本/commit、D2 SJTU 浅镜像 + 浅镜像边界）；`manifests/components.lock.toml` 的 `llvm-project` 条目 `enabled=true` + commit `6dfe1677ab8dffbc6ec13d53a1e0215d75147689`；`components/llvm-project/patches/series` 占位；`Makefile` 的 `build-mc` 真实 cmake+ninja（含 `llvm-lit FileCheck`）+ 文案同步；`make fetch` 走 SJTU 短路生成 `.work/source/llvm-project`。reviewer 第 1 轮 Needs Revision（B1 完成区 6/7 口径 + B2 Makefile stub/placeholder 陈旧文案 + #4 简写 + #7 命令）→ 返工 → 第 2 轮 **Accepted（7/7）** + architect 交叉复核确认（F1 ADR 措辞/F2 末尾换行/#5 依赖补 `INFRA-013t`/⑧ 完成区描述 由主会话修正；F3 已 Accepted 历史示例不改）。DADAO configure 失败属预期（ADR-0007），真实构建待 `LLVM-003t` | engineer + architect |
| 2026-09-17 | `LLVM-003t`：Triple 注册 + 最小 target 骨架——`0001-dadao-triple-registration.patch`（`Triple.h`/`Triple.cpp` + **`llvm/CMakeLists.txt` 的 `LLVM_ALL_TARGETS` 加 `DADAO`**）+ `0002-dadao-target-skeleton.patch`（13 新建 + 2 修改：DADAO target 骨架 / AsmInfo 大端 / ELF writer 存根 / 空 MCRegInfo 存根）+ `series`；`tests/lit/MC/Dadao/{triple-smoke.s,lit.cfg.py}`（退化路径：`llvm-mc --version` + FileCheck）；`Makefile` 的 `build-mc` 去掉非 ninja 目标的 `llvm-lit`、注释同步。`make prepare && make build-mc` PASS；`llvm-mc --version` 含 `dadao - DADAO SimRISC`；lit PASS；无仓库污染。reviewer **Accepted**（F1 `getLittleEndianArchVariant` 缺 dadao case / F2 `--triple` SIGABRT / F5 完成区错值 / F6 注释过时 已修；F3 ELF writer 死代码 / F4 `e_flags` 未设置 deferred）+ architect 交叉复核确认；**ADR-0007 升 Accepted** | engineer + architect |
| 2026-09-17 | 日志目录迁移：`.tao/logs/` → **`.work/log/<模块>/`**（311 文件，按任务 ID 前缀分桶：testcases 158 / infra 77 / spec 61 / llvm 15，文件名不变）；规则改记于 `.tao/README.md`「目录」节（`<任务ID>-<命令名>.log`，reviewer 重跑加 `-review-`，`tee` 同时输出终端与日志）；`mirrors.md` 与 `adr-0002`（D2 正文 + `## 修订` rev. 2026-09-17）同步；删除 `.tao/.gitignore`（仅含 `logs/`）；历史记录中的旧路径不改写 | 主会话 |
| 2026-09-18 | `LLVM-004t`：Register TableGen——`0003-dadao-register-info.patch`（256 RegisterDef + 6 RegisterClass + `DADAOFrameLowering` 存根 + `DADAOTargetMachine` 基类修复 `LLVMTargetMachine`→`CodeGenTargetMachineImpl` + `GET_REGINFO_*` 展开修正 + `getReservedRegs` 设 `rb1`/`rb2`）；**`Makefile` 的 `build-mc` 新增 `LLVMDADAOCodeGen` 目标（修复门禁盲区：原 `build-mc` 不构建 CodeGen，致 003t 的 CodeGen 骨架自始不可编译却未被发现）**。reviewer 第 1 轮 **Needs Revision**（B1 `.inc` 展开错 + B2 已删 API + B3 门禁盲区 + N2）→ 返工 → 第 2 轮 **Accepted**（6/6）+ architect 交叉复核确认（无需 ADR；F1 4 文件缺末尾换行 / F2 `FrameLowering` 抽象类 M2 阻塞 → deferred；F3/F4/F5 任务书精度由主会话补全） | engineer + architect |
| 2026-09-18 | `LLVM-005t`：指令格式 TableGen——`0004-dadao-instrinfo.patch`（`DADAOInstrFormats.td` 9 个 M1 格式类 + operand→`Inst` 位域绑定；`DADAOInstrInfo.td` **178 个 M1 指令 `def`**（脚本 `tools/llvm/generate_instrinfo.py` 从 `contracts/opcodes.yaml` 生成，`excluded_m1` 78 条不定义）；`DADAOInstrInfo.{h,cpp}`；`CMakeLists.txt` 增 `-gen-emitter`/`-gen-disassembler`（只生成不 include，使门禁覆盖））；校验脚本 `tools/llvm/validate_instrinfo.py`（按格式类分块校验位域绑定）入库。reviewer 第 1 轮 **Needs Revision**（9 格式类无位域绑定 → `-gen-emitter`/`-gen-disassembler` 失败；同类门禁盲区）→ 返工补齐 + 入门禁 → 第 2 轮 **Accepted**（6/6）+ architect 交叉复核确认（无需 ADR）→ 第 3 轮返工修 M1 注释/M2 validator 强化/M3 清理 | engineer + architect |
| 2026-09-18 | `LLVM-006t`：AsmParser + CodeEmitter——`0005-dadao-asmparser.patch`（`AsmParser/DADAOAsmParser.cpp` + CMakeLists、`DADAOMCCodeEmitter.cpp`、`DADAOMCInstPrinter.cpp`、`DADAOAsmBackend.cpp`、`DADAOFixupKinds.h`（`DADAO_FK_PCRel_12/_18/_24`）、`MCTargetDesc` 注册、`DADAOInstrInfo.td` 的 `ParserMatchClass`/`PrintMethod`）；仓库 lit `tests/lit/MC/Dadao/basic-encoding.s`（非 0 偏移前向 + 后向分支）；`tools/llvm/{gen_m1_asm,test_m1_asm,test_encoding_oracle}.py` 入库。**关键修复**：`applyFixup` 双偏移（`Data[0..3]`）+ 按位宽掩码（12/18/24）；`illi` 可汇编。reviewer 第 1 轮 Needs Revision → 第 2 轮 Needs Revision（B1 双偏移 + B2 掩码 + B3–B6）→ 第 3 轮 **Accepted**（6/6 + 178 全可汇编 + 带标签分支正确）+ architect 交叉复核确认（无需 ADR；F1 补丁内不可执行 lit → 删；F3 注释订正；F2 `LLVM-007t` scope 收窄；F4 `rela.si`/②③ deferred） | engineer + architect |
| 2026-09-18 | `LLVM-007t`（**重排后** = 反汇编器；原 `008t` 内容，原 `007t` 全量 lit 变为 `008t`，原 `009t` 归并进新 `008t`）：`0006-dadao-disassembler.patch`（`DADAODisassembler.cpp` 277 行：9 格式 + 9 个 `DecoderMethod` C++ 体 + 寄存器类解码 + 大端 `read32be`/`decodeInstruction`；`Disassembler/CMakeLists.txt`；顶层 `add_subdirectory`；并订正 `.td` 注释 `LLVM-008t`→`LLVM-007t`）；仓库侧 `tests/lit/MC/Dadao/disassembly.s`（15 条、注释含位域推导、CHECK **钉字节**）+ `lit.cfg.py` 加 `%llvm_objdump`。`make_patch.py` 重生成 `0001`~`0005`（元数据 + `0005` 一处 `.td` 行序调换；reviewer 验证新旧 postimage sha1 相同）。reviewer 第 1 轮 Needs Revision（F1 `set.zw` 推导错 / F2 `--list-targets` 非法 / F3 验收标准 5 冲突 / N1–N3）→ 返工 → 第 2 轮 **Accepted**（6/6 + 178 往返 + 15 条四方比对 + 反例证明 CHECK 有效）+ architect 交叉复核确认（无需 ADR；F1 `.td` 旧编号已在 0006 订正、F2 交付物补全、GPRF 告警/`--triple` 约定登记 deferred） | engineer + architect |
| 2026-09-19 | `QEMU-002t`：QEMU 组件基线——**ADR-0008**（基线 **v11.1.1**，commit `c3d48b7d1e89604920e5b81b91140c2ad39a1943`，tag 对象 `5e35f266…` 仅溯源；来源 SJTU 浅 bare 镜像 `.cache/qemu.git` 51.78 MiB；Candidate→**Accepted**）；`manifests/components.lock.toml` 的 `qemu` 条目 `enabled=true` + commit + `[[component.source]]`=sjtu；`components/qemu/patches/series` 空占位；`Makefile` 仅校订 help 一行（`build-qemu` 自 `INFRA-006t` 起已是真实 recipe）；真实 `./configure --target-list=riscv64-softmmu --enable-tcg` exit=0。reviewer 第 1 轮 Needs Revision（ADR 把 0628 的 tag 对象当 commit + 完成区 `git diff` 数字不实 + **伪造 `ls` 输出**）→ 返工 → 第 2 轮 **Accepted**（10/10）+ architect 交叉复核确认（F1 ADR 升 Accepted；F2 `QEMU-001k` ADR 编号笔误；F3 `QEMU-003t` 的 v11.x `tcg/translate.c` 路径；F4 `QEMU-014t` 构建路径；`deferred.md` 新增 3 条：`make doctor` 不查 QEMU 构建依赖 / `QEMU_BUILD` 落点 / v11.x 目录变更） | engineer + architect |
| 2026-09-19 | `QEMU-003t`：Target Skeleton——`0001-dadao-target-skeleton.patch`（**20 文件/954 行**：`target/dadao/` 9 + `hw/dadao/` 3 + `configs/{targets,devices}/dadao-softmmu*` + 6 处共享集成含 `hw/meson.build` 的 `subdir('dadao')`、`qapi/machine.json`、`include/qemu/base-arch-defs.h`）+ `series` + `Makefile` 的 `build-qemu` 改 **out-of-tree**（`.work/build/qemu/`）+ `tools/qemu/smoke-dadao-m1.sh`。`make build-qemu` PASS（2363/2363），`qemu-system-dadao -M ?` 含 `dadao-m1`，冒烟 **ILLI 0x88=136**；CPUState `rd[64]/rb[64]/rf[64]/ra[64]/pc`，复位值符合 ADR-0004 D2（rf0 从 `SimRISC-00` 位段独立复算一致），exit port 按 D3、内存图按 D1。reviewer **Accepted**（8/8；QMP 实测复位值 + `info mtree` + 6 条自造非零指令全 ILLI + `alpha-softmmu` 交叉构建）+ architect 交叉复核确认（无需 ADR；F1 `IS_LITTLE_ENDIAN`→`TARGET_BIG_ENDIAN`、F3 `TCGCPUOps` 4 个非 NULL 断言、交付物清单补全由主会话修；deferred：3 文件缺末尾换行、`tlb_fill` 的 `access_type`、**`make prepare` 非幂等**（infra）、`QEMU_BUILD` 条目已消解、`QEMU-014t` 构建路径更新） | engineer + architect |
| 2026-09-19 | `QEMU-004t`：Decodetree 解码——`0002-dadao-decodetree.patch`（`insn.decode` 274 行/**256 pattern** + `translate.c` +1818/-33 + `meson.build`）、`tools/qemu/generate_decodetree.py`/`validate_decodetree.py` 入库（默认输出 `/tmp/opencode/QEMU-004t/`，不写仓库）；**N-1 跨模块修正（用户授权）**：`tools/spec/generate_opcodes.py` 3 处 + 重生成 `contracts/opcodes.yaml`（78 条 `excluded_m1` `decode: UNDI`→`ILLI`，与 ADR-0004 D5.1 对齐）。reviewer **Accepted**（7/7；自写解析器 256↔256 全等、独立运行上游 `decodetree.py`、运行时实测 UNDI 0x89/ILLI 0x88/子表嵌套/`swym` NOP 用 TB 指令数判别）+ architect 交叉复核确认（无需 ADR；F1 `%op` 未声明=设计选择、F2 MISC op 值错位、F3 `SPEC-003t` 历史表述不改写、N-1 条目标已消解） | engineer + architect |
| 2026-09-19 | `QEMU-014t`：QEMU 语义测试 harness——`tests/scripts/`（`build_test_binary.py`（raw `encoding.word`→4 段 loader/test/dumper/exit，含 `rb2rd` 读 RB 与 PC）+ `run_qemu_test.py`（**只看 `$?`**，内置 QMP 客户端）+ `gen_trampoline.py` + `trampoline.bin`（16 B）+ `verify_harness_dump.py`（通道验证脚本，入仓）+ `README.md`）+ **ADR-0009**（Candidate；D1–D7 **用户逐条确认**：raw-encoding 不依赖 `llvm-mc` / guest 内 XOR+ORR 比较（dumper 仅 FAIL 诊断）/ ROM trampoline（`jump rbX,rd0,0` 绝对跳转，`jump imms24` 相对跨距不足）/ scratch `rd60`–`rd63`+`rb60`–`rb63`（RA 不作 scratch）/ 退出码协议 `0x00`=PASS、`0x01`–`0x7F`=FAIL、`0x80`–`0xFF`=机器 fault / `expected_pc`·`expected_state.ra`·`encoding.reserved` 消费契约 / 诊断模式 guest 自旋（`imms12=0`）+ QMP `pmemsave`）。**关键修复**：用 `rb2rd` 补齐 RB 比较与 PC dump；QMP `pmemsave` 文件路径须加引号（HMP 把路径前导 `/` 当除号，错误被 `"return"` 静默吞掉）、`subprocess.Popen`+`wait` 保活 QEMU 至 host 主动 dump 并 `quit`；清除 4 条 `check(...,True)` 恒真断言（完成区「无恒真断言」曾不实）；state-dump 布局定案（`rd[1..63]@+0x008`、`rb[1..63]@+0x208`、`pc@+0x400`、保留槽 `+0x000`/`+0x200`，共 1032 B）。验收：2/4/5(生成+加载)/6/7/9/10a PASS（10a 由 reviewer 用不同 pattern 独立复现，逐字节 mismatch=0）；1/3/5(进入 BINARY_BASE)/8/10b 待 `QEMU-005t` 语义。reviewer **五轮** Needs Revision→第 5 轮 **Accepted** + architect 交叉复核确认（无需新 ADR；3 项低severity 残余已在收尾轮修掉：`build_dumper_section` docstring 偏移注释、README Files 表未列新脚本、`verify_harness_dump.py` 偏移改为从 `build_test_binary` 派生常量） | engineer + architect |
| 2026-09-19 | `QEMU-005t`：RD 整数语义——`0003-dadao-rd-arith.patch`（12 指令族 / 97 insn：128 位 rrrr 加减乘、定宽 orrr 加减乘除余、`add.si`、`cmp.*`、逻辑、移位/扩展、`cs.*`、`set.ow`/`set.zw`/`or.w`/`andn.w`、`rd2rd`；范围外保持 ILLI 桩）+ `series` + `tools/qemu/min_rom_probe_005t.py`（最小 ROM 探针：UNDI 终止符区分 136/137 + 精确值比较 + CTL 自检）+ `tools/qemu/check_005t_coverage.py`（重写为真读 `translate.c` 的桩检查）。**关键修复**：①运行时条件异常路径**不得**设 `NORETURN`（B1：合法 `div.uo` 曾 SIGABRT `cpu-exec.c:911`）；②`helper.c` 改 `cpu_loop_exit_restore(cs, GETPC())`（N2：异常后 TB 起点重放 → 退出码被后续 UNDI 覆盖且非确定）；③`gen_zero_extend` 的 `1ULL<<63` UB（N1：`div.uo`/`rem.uo` 恒 0）；④`ext.*_orrr` 按 `rdhd` **值**取起始位 + `hd>N`→ILLI 须先于掩码；⑤定宽 `add/sub/mul.{sb,sw,st}` 符号/零扩展（B3/B4）；⑥`translate.c:243` `int_min` UB。验收：`make build-qemu` PASS、探针 20/20 + CTL OK、`git am` 干净；语义由**独立 oracle**（`-d cpu` 寄存器 dump 111/111 + 仓库独立向量回放 **220/220**）承担。reviewer **四轮** Needs Revision→Accepted + architect 复核确认（`AGENTS.md` 新增「验证脚本反例门控」；harness 端到端顺延 `008t` 后统一复跑） | engineer + architect |
| 2026-09-19 | `QEMU-006t`：RD 存取与 MALIGN（合并任务，ADR-0010 D3）——`0004-dadao-load-store.patch`（22 条 RD 单/多 load/store + MALIGN 精确异常 `EXCP_MALIGN`/`cpu_do_unaligned_access`/`MO_ALIGN_N` + 前置的 `jump-rrii`/`br.nz`；并修改 003t 产物 `helper.c`/`hw/dadao/dadao-machine.c`：exit port `.impl.max_access_size=8`、ROM store 按 D5.6 返 ILLI）+ `series` + `tools/qemu/min_rom_probe_006t.py`（34 用例 + 3 CTL）。**四轮 reviewer 修复**：J1 `jump-rrii` 缺 `tcg_gen_exit_tb` 致 SIGABRT；J2 `rb0` 须用**翻译期 PC**（`env.rb[0]` 在 TB 内陈旧）；J3 `st.o-rd` 缺 `MO_ALIGN_8`；J4 `stm.*`→exit port 须运行期 ILLI；J5 ROM store 按 D5.6 返 ILLI；J6 **MALIGN 优先级须高于访问种类**（`gen_check_exit_port_illi` 加自然对齐门控）。验收：`make build-qemu` PASS、探针 34/34 + CTL、reviewer 独立探针 **46/46**、005t 回归 20/20、`git am` 干净；harness e2e 待 `020t` 回补 | engineer + architect |
| 2026-09-19 | `QEMU-007t`：`translate.c` 拆分重构（ADR-0010 D2）——`0005-dadao-translate-split.patch`（把 3685 行/256 个 `trans_*` 按 §-级族拆入 `target/dadao/insn_trans/` 的 **10 个 `.c.inc`**：`arith`/`compare`/`logic`/`shift`/`extend`/`cond_assign`/`imm`/`block`/`mem`/`ctrl`；`translate.c` 只留 decodetree include + helper + 寄存器宏 + 10 个 `#include`）。**纯重构**：reviewer 用自写解析器做「256/256 函数体逐字节一致」+ 探针回归（005t 20/20、006t 34/34）+ 三类注入反例证明比对非恒真；并明确**对象级 diff 不可作判据**（`decodetree.py` 对 `PYTHONHASHSEED` 非确定 + `__LINE__` + 内联）。收尾修「树/补丁不一致」：`.work/source/qemu` 的 0005 提交 amend 为 `99a4dda`（去 10 个 `.c.inc` 尾部空行）、补丁重生成后 diff 一致、临时 worktree 树 == 树 HEAD。reviewer **Accepted** + architect 复核确认（R3 `decodetree.py` 非确定性登记 `deferred.md`） | engineer + architect |
| 2026-09-19 | `QEMU-008t`：控制流 + RB——`0006-dadao-ctrl-flow.patch`（`br.eq`/`br.ne`、`br.n`/`br.nn`/`br.z`/`br.nz`/`br.p`/`br.np`、`br.z-rb`/`br.nz-rb`、`jump-iiii`、`call-iiii`/`call-rrii`、`ret`、`rela.si`、`swym` + RB 存取/块复制/立即数/**全 64 位算术**/`cmp.uo-rb`；按 ADR-0010 D2 写入 `trans_ctrl`/`trans_mem`/`trans_block`/`trans_arith`）+ `series` + `tools/qemu/min_rom_probe_008t.py`（38 用例 + 3 CTL）。**三轮 reviewer 修复**：F1/F2 RegRAS 压/弹栈移位方向相反；F3 RASOF 深调用不可达；F4 RASOF 前已写 RA（违反 §9.2 精确异常）；F5 探针 T21–23/T27–29/T33–35 FAIL 路径不可达（恒 PASS，致「`add.so-rb` 截断 48 位」检不出）；F6 T16 假嵌套；F7 完成区不实；N1 反例表数字/FAIL 集不实；N2 工作区污染致「38/38」不可复现（实为 37/38）。验收：`make build-qemu` PASS、探针 38/38 + CTL、`-no-shutdown`+`info registers` 直读 RA 验证压/弹栈方向与 RASOF 精确异常、落点 19/19 逐字节、005t/006t 回归 20/20+34/34；harness e2e 待 `020t`。MemRAS/`ra0` 限制登记 `deferred.md`→`013t` | engineer + architect |
| 2026-09-19 | `QEMU-009t`：`rela.si` 基址**定向回归验证**（**验证任务，不改补丁**）——新增 `tools/qemu/min_rom_probe_009t.py`（12 用例：`cmp.uo-rb` **精确值断言**，期望值由 Python 按 §4.7 独立算；4 类注入门控全部检出）。**结论**：`trans_rela` 与 §4.7 一致（基址取 `rb0`(PC) 非 `rb[ha]`、`(PC & ~0xFFF) + sext(imms18<<12)`、高 16 位保持、`ha==0`→ILLI），**无缺陷**；reviewer 独立 `-d cpu` oracle `MISMATCH=0`。**两轮 reviewer**：第 1 轮打回「探针只验不崩溃、公式注入可逃逸」→ 加精确断言后第 2 轮 **Accepted** + architect 复核确认。**流程事件**：engineer 子代理连续 3 次空返回未落盘（按规则停止重试），由**主会话代行**修改探针，仍经 reviewer 验收；`AGENTS.md` 新增「连续空返回的处置」规则、`deferred.md` 登记该事件 | 主会话 + reviewer + architect |
| 2026-09-19 | `QEMU-010t`：`ldm.o-rb` 实现 + **补 008t 漏项 `stm.o-rb`**——`0006-dadao-ctrl-flow.patch`（`git format-patch` 重生成；`trans_ldm_o_rb`/`trans_stm_o_rb` 对称：EA 48 位截断、**循环内逐次再掩码**、`MO_BEUQ\|MO_ALIGN_8`、ILLI 先于写、未对齐 MALIGN）+ `tools/qemu/min_rom_probe_010t.py`（28 用例 + 反例门控）。**五轮 reviewer**：P1 补丁手工拼接致 `git am` 失败 + P2 `stm.o-rb` 仍是 ILLI 桩（008t 验收 4 误标 PASS）→ 探针 `cmp_uo` 误用致语义断言**恒真** + EA 截断判别用例缺失 → 循环内未逐次掩码（architect 补充发现）→ T28 首断言 `br_nz(18,4)` 指向 PASS 分支（恒真）→ 完成区文档更正。**探针分支偏移/极性错误本模块已 3 次**：规则固化入 `AGENTS.md`「探针分支须双向验证」、标签化探针框架建议入 `deferred.md`；008t 验收遗漏亦登记 `deferred.md` | engineer + architect |
| 2026-09-19 | `QEMU-011t`：`div.*`/`rem.*` label 顺序**定向回归验证**（**验证任务，不改补丁**）——新增 `tools/qemu/min_rom_probe_011t.py`（28 用例 + CTL；`cmp.uo`+`div.uo` 精确值机制；`rd2rb`+`rb2rd` 回读校验）。**结论**：label 结构正确（`gen_set_label` 位置、无 dead code）、除零与各 size `INT_MIN÷−1` → ILLI `0x88`、truncate-toward-zero 与余数符号=被除数符号均正确，**无缺陷**；3 类注入（label 调换/去零检查/去溢出检查）均 FAIL + 还原重建复绿。**四轮 reviewer**（含主会话两次代行）：①探针 `_set_rd_to_val` 用 `add.si`（仅 18 位）构造 64 位值致被除数静默变 0 → 误报 4 条失败；②**陈旧二进制未重建** → 误把 005t 探针失败当成真实回归；③engineer 误判「`br.ne` 不工作」（最小复现偏移算错）；④回读块 `br_ne(22,23,3)` 偏移错（基址=分支自身，落到 `div_fn`）→ 失配不 FAIL 且掩蔽构造错误，修为 `off=4`。教训固化：`AGENTS.md` 补「还原须含重建」「探针值构造须回读校验」「探针分支须双向验证」；标签化探针框架建议入 `deferred.md` | 主会话 + reviewer |
| 2026-09-19 | `QEMU-012t`：分支 PC 公式 + `call` RA 压栈 §5.6.1 **定向回归验证**（**验证任务，不改补丁**）——新增 `tools/qemu/min_rom_probe_012t.py`（13 用例 + CTL；分支 taken 正/负偏移、not-taken、`br.eq`/`br.nz-rb`、`call-iiii`/`call-rrii` 返回地址、`call→ret→landing`、RA §5.6.1 三情形）。**结论**：§5.2/§5.4/§5.5/§5.6 公式与实现**一致、无缺陷**。**下发前预检**发现并修正任务书 4 处（§5.6.1/§5.6.2 章节号、BLOCKED 归因、harness 命令、验收替代）。**两轮 reviewer**：F1 R2 误标 §5.6.1 case 2（`[call_iiii(1)]*64` 实为**异址** 64 深 → 走 case 3 溢出）→ 改**同址递归**（reviewer 的 E3 构造）+ 新增 R2b 异址对照，注入「case 2 禁用」→ R2 0x8A FAIL 而 R2b 不变，成对判别力成立；F2 R1 往返不能证 high16=0x0001；F3 R4 实为 2 级；F4 B2 未钉住负偏移。**实测确认 harness 控制流能力缺口**：`ctrl-br/jump/call` 的 encoding+semantic 全 TIMEOUT、`ctrl-ret` semantic 因 loader 的 `rd2ra` 为 ILLI 桩（属 `013t`）报 ILLI → BLOCKED 归 `017t`/`018t`/`013t`（**非** `020t`）。`-d cpu` 可直读 `ra63`（reviewer oracle B4 `count==3`/B5 `count==1`）。architect 交叉复核判「可收尾」（§5.3 `jump` 未覆盖为非阻断，`006t`/`008t` 已覆盖） | 主会话 + reviewer + architect |
| 2026-09-21 | `QEMU-013t`：**RA 指令语义**（`0007-dadao-ra-semantics.patch`）——`ld.o-ra`/`st.o-ra`/`ldm.o-ra`/`stm.o-ra`（8B 对齐 MALIGN、`immu6=0`/范围越界→ILLI）、`rd2ra`/`ra2rd`（`immu6` 1–63、`ra2rd` 目的 rd0→ILLI）+ **补全 MemRAS**（`ra0` 低 48 位指针/高 16 位计数，§5.6.1/§5.6.2 case 3 的 spill/restore）。新增 `tools/qemu/min_rom_probe_013t.py`（22 用例 + `run_test_dcpu`/`last_ra_dump` 回读 + CTL）。**下发前预检**修正任务书：删除「RA 向量补充」（违反 Independent oracle，向量缺口登记 `deferred.md`→TESTCASES）、明确 MemRAS 范围、补替代方式。**四轮 reviewer**（第 1 轮 Needs Revision → 第 4 轮 Accepted）：**D1[阻断]** `gen_ras_pop` §5.6.2 case 3 的 RASUF **非精确异常**（先写 `ra0` 再判 `mem_hi==0`）→ 有效性判定前移到任何 RA 写入之前，`-d cpu` 实测 fault 时 `RA[00]` 未变；F2 补 X1–X3（MemRAS spill 溢出/计数下溢/内容无效）+ `ra0` 回读；F3 `rd2ra`/`ra2rd` 源目的分属不同寄存器组，§4.9.3 重叠规则为**空条件**（降序注入仍全绿是正确结论）；F4 B2 同源互比无判别力，且 **`br_nz` 落点越位一格致恒 PASS**（M1/M3/B3/B9 同类，修一类；本模块**第 5 次**分支偏移事故）；**R1 空测试**（`call_iiii(3*65)` 直接跳过 64 组链式 call，MemRAS 从未被触碰；`-d cpu` 独立复核 133 TB/`ra0.hi16` 轨迹 `0→1→2`）；X4 断言未观测自身主张的 `ra63` 存回 + 落点 `0x0048`→`0x0044`（`words()` 只生成 3 条指令），改为 `ra2rd` 回读 ra63 + `cmp.uo` 比对。注入验证：引用计数不减/不存回/PC 取错均使 X4 FAIL。基线 **22/22**、012t 13/13、008t 38/38、`git am 0001→0007` 干净 + tree `5f8cd927…` | 主会话 + reviewer |
| 2026-09-21 | `QEMU-015t`：**harness 语义验证修复**（`tests/scripts/build_test_binary.py` 普通模式不 emit dumper = ADR-0010 D1 修法 a 落地；`run_qemu_test.py` CLI fail-closed：0 case/全 SKIP → `exit=2`）。**成果**：普通模式对 RD-only 语义向量恢复 PASS（修复前 TIMEOUT）；反例门控（篡改 `expected_state` → FAIL/`exit=1`，还原 → PASS）；batch `597 total, 562 passed, 29 failed, 5 deferred, 1 error`。**验收三轮**（第 1 轮 Needs Revision → 第 3 轮 **Accepted**）：**reviewer 证伪 engineer 根因**（原称「dumper 131 条指令导致 TCG 翻译超时/代码量问题」）——合成程序证明 96 条 `st.o-rd`→`exit=0`、**100 条→`exit=124`**；500 条 `set.zw`→0、**510 条→124**（`TCG_MAX_INSNS=512`）；`-d exec` 显示 base TB 6s 内执行 **46185 次**、PC 恒 `0xFFFF00000000` ⇒ **执行期死循环**。**根因 = QEMU dadao target TB 续接缺陷**：`dadao_tr_tb_stop` 用 `tcg_gen_goto_tb(1)+tcg_gen_exit_tb(NULL,0)` 且**缺 `gen_update_pc`**（对照 riscv `gen_update_pc → goto_tb → exit_tb(tb,n)`）⇒ TB 被切后 `env->pc` 不更新，下一 TB 从旧 PC 起。**影响面 = 任意长 TB 的 guest 程序**（此前探针/向量因 TB 短未触发）。处置：新建 `QEMU-022t`（补丁 `0008`，TB 缺陷根治）+ `020t` 缩窄为「TB 安全分段 dumper + `--dump` 端到端验收」+ `021m`/`001k`/`milestones.md` 同步；`015t` 验收 #6（`--dump` 的 `rb`/`pc`）**BLOCKED**，待 `022t`+`020t` 交付后回补；29+1 条 batch 失败逐条归类（24 `mem-rd` 向量/harness 内存模型不一致 → TESTCASES；3 `ctrl-call`+1 `ctrl-ret` harness PC 布局缺口 → `017t`/`018t`；2 `misc` = `fence` ILLI 桩 → 登记 deferred）；另修正根因表述 3 处（TB 切点 = op buffer 而非 512；workaround 阈值 = op-buffer ≈40 条） | 主会话 + reviewer + architect |
| 2026-09-21 | `QEMU-022t`：**TB 续接缺陷修复 + exit-port 可靠 halt**（`0008-dadao-tb-chain-fix.patch`，单原子补丁 3 文件）——`translate.c` 补 `gen_update_pc` + `translator_use_goto_tb` 守卫；`hw/dadao/dadao-machine.c` exit-port handler 改 **`cpu_loop_exit`**；`target/dadao/helper.c` `dadao_cpu_do_interrupt` 加 shutdown 守卫。**根因链**：TB 被切（TCG op buffer ≈100 store-heavy 条 / `TCG_MAX_INSNS=512`）后 `env->pc` 不更新 → 执行期死循环（`-d exec` 46185 次、PC 恒 `0xFFFF00000000`）。**第 1 轮 reviewer 判 Needs Revision**：TB 修复正确但暴露 **exit-port 退出码竞态**（`cpu_exit()`+`EXCP_INTERRUPT` 不能停外层 `cpu_exec`；最小复现 40 次中 5 次不一致；`008t` 3/10、`009t` 7/10、`010t` 2/3、`013t` 2/3 假失败）。经 architect 规划 + 用户逐条确认 **`ADR-0011`（Accepted，D1–D4）**：exit-port 写后立即 halt（`cpu_loop_exit` longjmp）、exit code 优先于后续 fault、补丁不可分离（S1 实测证明）、ADR-0004 D3 增补「写入后立即停止 guest 执行」。**第 2 轮 reviewer Accepted**：独立复现 **2100 次全一致**；`008t`/`009t`/`010t`/`013t` 各 ×6 全绿确定；两个反例门控（注释 `gen_update_pc` → TIMEOUT；注释 `cpu_loop_exit` → 40 次不确定）均可达；`--dump` 的 `rb`/`pc` 恢复（`pc=0xffff00000210`）。遗留：longjmp 泄漏 MMIO re-entrancy 守卫（登记 deferred）、提交作者身份（登记 deferred） | 主会话 + reviewer + architect |
| 2026-09-21 | **`QEMU-020t` 关闭（用户裁定）**：该任务（harness dumper 改造）已无必要——交付物 #1（普通模式不 emit dumper）由 `QEMU-015t` 落地；原拟的「TB 安全分段 dumper」因 `QEMU-022t`（`ADR-0011`）根治 TB 续接缺陷而不再必要（单段 dumper 已能正确导出 `rb`/`pc`，见 `015t` 验收 #6 回补）。**处置**：删除 `QEMU-020t-harness-dumper改造.md`（编号 `020` 留空，不重编）；同步 `QEMU-001k`（任务表/依赖关系）、`QEMU-021m`（关联任务）、`milestones.md`、`MEMORY.md`、`deferred.md`；`ADR-0010` D3 表就地补注（用户确认）记录该历史。 | 主会话 + architect |
| 2026-09-21 | `QEMU-016t`：**harness `expected_state.memory` 比对**（`tests/scripts/build_test_binary.py`：新增 `encode_ld_ub/uw/ut/o`（op 0x10/0x11/0x12/0x20）+ `derive_width_from_mnemonic()` + 在 `build_exit_section()` 的 RA 比对循环后、退出码分支前插入 memory 比对循环；地址用 `MEM_RB`(61)（**不得**用 `TEMP_RB`(60)，它存 exit port 地址），实际值经宽度对应**无符号** load 读入 `DUMP_RD`(63)，与期望值 XOR 后 ORR 进 `ACCUM_RD`(61)）。**下发前预检订正任务书 6 处**（`emit_state_compare()` 不存在→实为 `build_exit_section()`；`n_patch`/guard patching 节 v5 无；`load_reg`/`TEMP_RB` 误用；依赖 `TESTCASES-006t`→`004t`；验收 5 项 BLOCKED 归因已关闭的 `020t`）。**engineer 第 1 轮有阻断缺陷且完成区结论不实**（`derive_width_from_mnemonic` 无条件调用 → 224 条非 store 向量 `ValueError`；完成区却报「无新增失败」）→ 主会话实测证伪后退回；第 2 轮修为 `if memory:` 守卫。**reviewer 独立验收 Accepted**（全量 `597/562/29/5/1` 与 HEAD 基线逐字节一致、24 条 store 向量逐条 PASS、反例门控 篡改→FAIL/还原→PASS、反向注入 实际值≠期望→FAIL、宽度映射反解析二进制核对、多 entry 合成「只错第二条」→FAIL、退出码分支零改动）；F1/F2 两处文档不实已更正。**验收 #6 仍 BLOCKED**（`mem-rd` 24 条窄 load 向量与 ISA 冲突 → TESTCASES）。 | engineer + reviewer |
| 2026-09-21 | `QEMU-017t`：**分支语义 harness 扩展**（`tests/scripts/build_test_binary.py`：新增 `encode_illi()`、`encode_jump_iiii()`、`build_branch_test_binary()`；`expected_pc is not None` 时调度）。**契约**：`expected_pc` 按 **delta**（= `expected_pc - BINARY_BASE`）消费，`delta==8`→taken / `delta==4`→not-taken（用户裁定，补注 `ADR-0009 D6`）；v5 分支基址=**分支自身地址**，故 not-taken 的 poison 偏移为 **`+2`**（0628 的 `+1` 不适用）。**预检订正任务书 4 处**（`emit_state_compare()` 不存在、not-taken 偏移 `+1`→`+2`、范围「改向量」→纯 harness、BLOCKED 归因 `008t`/`020t` 过时）；**新发现**：20 条 `ctrl-br` semantic 改前**全部真空 PASS**（`expected_pc` 被忽略 + `expected_state` 空）。**engineer 第 1 次空返回 + 第 1 版 not-taken poison 不可达（恒真）**（`imm=2` 目标 = branch+2 = exit 段内部，illi 放在 exit 段后永不可达）→ 主会话实测证伪（`taken 输入+谎报 delta=4` 仍 PASS）后退回；第 2 版加单字 trampoline `[branch imm=2][jump imms24=2][illi][exit]`。**reviewer 独立验收 Accepted**（双向反例门控均 FAIL、22/22 字节级布局核对、偏移敏感性 imm=1 假 PASS、22 semantic+12 encoding 全 PASS、全量 `597/562/29/5/1` 与基线 diff **IDENTICAL**、约束 md5 核对）。**交接 `018t`**：`ret`（delta=4）不适配 not-taken layout，须专用 layout。 | engineer + reviewer |
| 2026-09-21 | `QEMU-018t`：**call/ret 语义 + RA 重定位**（`tests/scripts/build_test_binary.py`：`build_call_ret_binary()` 合成 `[call imm=2][landing 跳板 jump→exit][ret][exit 段]` 往返；`build_exit_section(relocate_ra=...)` 对 `expected_state.ra` 低 48 位加 `loader_bytes`；`build_branch_test_binary` 对 call 同步重定位 `input_state.ra`；`build_loader` 改返回 `(words, len)` 元组；路由 `ret`→`build_call_ret_binary`）。**预检订正任务书 5 处（P1–P5）**：`expected_state.ra` loader 重定位（RA=`<count:16><返回地址:48>`，返回地址=call 自身地址+4，实测 `trans_ctrl.c.inc:607`）、`ret` 合成往返（改前 `ctrl-ret[0]` preload 虚构 RA → 跳进 loader 区 → TIMEOUT）、`ret` 路由不得复用 not-taken layout、BLOCKED 归因 `012t`/`020t` 过时、范围=纯 harness；**补注 `ADR-0009 D6`（续）纳入 `expected_state.ra`**。**engineer 连续 2 次空返回 + 第 1 版 ret 布局错误**（`[call imm=1][ret][exit]` → 返回地址=ret 自身 → **RASUF 0x8B**）→ 主会话实测证伪退回；第 2 版改对。**reviewer 独立验收 Accepted**（全量 `597/566/26/5/0`（29 failed→26、1 error→0，零新增）、子集 `11/11/0/0`、字节级布局断言、**`-d cpu` 独立 oracle**（PC 轨迹 0→8→4、RA63 末态 `<2>…0014`）、往返门控、RA 重定位门控（关闭→`ctrl-call[2]` FAIL）、输入重定位正当性实测、`build_loader` 影响面核对）。 | engineer + reviewer |
| 2026-09-21 | `QEMU-019t`：**trans lint**（新建 `tools/qemu/check_qemu_trans.py`：由 `insn` 派生 `trans_*` 名并**复用 `validate_decodetree.py` 的 `build_unique_func_names()`**，在 `components/qemu/patches/*.patch` 中按 `static bool trans_<name>(` **精确匹配函数定义**，输出 MISSING 明细 + `256/256 (M1 178/178)`；默认 exit 0、`--strict` 有缺失 exit 1；支持 `--src`/`--patches`/`--yaml` 覆盖路径）。**预检订正任务书 6 处（P1–P6）**：命名由 `insn` 派生（旧文举例 `trans_add_uo`/`trans_br_z`/`trans_ld_o` **均不存在**）、须精确匹配避前缀碰撞（实测 `br.n`↔`br.nn/ne/np`、`cs.n`↔`cs.ne` 2 例）、复用 `validate_decodetree`（DRY）、计数全 256 + M1 子集 178、补反例门控（真实 0 缺失 ⇒ lint 永不失败）、验收 #4 归因 `007t` 过时。**实测基线**：256/256 一一对应、0 缺失 0 多余；`swym`/`illi` 也有 trans（旧文「MISC 无 trans」不成立）。**reviewer 独立验收 Accepted**（默认/`--strict` exit 0；反例 `add_sb`/`br_n_rd`/`cs_n_rd` 缺失 → MISSING + exit 1；`excluded_m1` 缺失 → 255/256 但 M1 仍 178/178；DRY 核对确认 import 复用且无硬编码；只读性核对）。**非阻断观察（已登记 deferred）**：`collect_trans_defs()` 对 patch 的 `+`/`-`/上下文行一视同仁，理论上前序 `+` 会掩盖后序删除，当前无风险。 | engineer + reviewer |
| 2026-09-21 | **方案裁决：`input_state.memory` 按宽度写入（用户裁定，方案 C）**。背景：`QEMU-015t`/`016t` 发现 24 条窄 load 向量 FAIL，根因 = harness 的 `build_loader()` **无条件用 `st.o`（8 字节）**写 `input_state.memory`，在大端下 `value=0x42` 的字节落在 `A+7` 而 A 处为 `0x00`。**候选方案实测**：**A**（改 memory 地址 `A→A+7`，任务书原描述）→ **不可行**（FAIL `MALIGN 0x8C`：load 地址由指令编码固定，且 `st.o` 要求 8 字节对齐）；**B′**（value 左对齐 `V<<(64−8N)`）→ 实测 PASS，但**左对齐与 ABI 的右对齐 slot 相反、亦非真实 BE 机器惯例 → 撤回**；**E**（按 ABI `va_arg` 规则改指令偏移 `A→A+8−N`）→ 实测 PASS，属 ABI-grounded 的数据修正；**B**（改 golden = 扩展位）→ 可行但测试意图退化；**C**（harness 按宽度写）→ **采纳**。**依据**：真实大端机器（MIPS/SPARC/PPC）类型化数据按自然宽度存放；`spec/DADAO-21-ABI §数据表示`「多字节数据最高有效字节在最低地址」，其「右对齐」仅适用于 8 字节参数/varargs slot。**处置**：新建 `QEMU-023t`（harness 按宽度写；向量零改动；48 条 `input_state.memory` 向量受影响，其中 24 条窄 load 由 FAIL 转 PASS）；**删除 `TESTCASES-010t`（窄 load 版）**——该号系 `009t` 为「154 数据级缺口消解」预留（见 `deferred.md`），本会话误占，删除后号位归还；同步 `021m`/`001k`/`milestones.md`/`018t` 历史引用；**补注 `ADR-0009`（续二）**记录 `input_state.memory` 契约。 | 主会话 + 用户 |
| 2026-09-21 | `QEMU-023t`：**harness `input_state.memory` 按宽度写入**（`tests/scripts/build_test_binary.py`：新增 `encode_st_b_rd`/`st_w_rd`/`st_t_rd`（op 0x18/0x19/0x1A）；`derive_width_from_mnemonic()` 扩为 3-tuple 返回 `(width, ld_fn, st_fn)`；`build_loader()` 按 mnemonic 宽度用 `st.b/w/t/o` 写 `input_state.memory`；`tests/vectors/schema.md` 补契约说明）。**契约**（用户裁定 + `ADR-0009` 补注续二）：`input_state.memory[].value` = 「`address` 处存放的 N 字节值」，N 由 mnemonic 推导。**候选方案实测裁决**：A（改 memory 地址）不可行（`MALIGN 0x8C`）；B′（value 左对齐）与 ABI 右对齐 slot 相反 → 撤回；E（改指令偏移按 `va_arg` 规则）可行；B（改 golden）意图退化；**C 采纳**。**成果**：24 条窄 load **FAIL→PASS**（reviewer 用 HEAD 版 harness 独立复现改前 24 FAIL）、48 条 `input_state.memory` 向量全 PASS、子集 `172/172`、全量 **`597/566/26/5/0` → `597/590/2/5/0`**（仅余 2 条 `misc` `fence` ILLI 桩）、18 条 8 字节场景**逐字节不变**、48 条地址对齐全满足、反例门控（宽度写错 → 8 条 FAIL）。**reviewer Accepted**。**约束偏差（已接受）**：op 值硬编码（与既有 helper 模式一致、reviewer 已核对取值正确）→ 建议加交叉校验 lint，登记 `deferred.md`。 | engineer + reviewer |
| 2026-09-21 | **`fence` 处置裁定（用户）**：`fence` 实现缺失（`trans_fence` 为 ILLI 桩，违反 `contract-isa §7.3`/`spec/SimRISC-04`/`007t` 的 NOP 约定）**放入 `deferred`，暂不建任务**。已完成完整分析（现象、根因、spec 依据、编码解码实测、建议修法、影响面）并写入 `deferred.md`。**后果**：全量 harness 基线保持 `597/590/2/5/0`，`QEMU-021m` **继续阻塞**（不得置「里程碑」）。 | 主会话 + 用户 |
| 2026-09-21 | `LLVM-008t`：**全量 lit（迁移 + 统一）**。**范围（用户裁定）**：新建 14 个格式族文件（`rrii_alu`/`rrii_branch`/`rrii_load`/`rrii_store`/`rrri`/`rrrr`/`riii_branch`/`riii_ret`/`iiii_jump`/`orrr`/`orri`/`rb_ops`/`rwii`/`oiii`，强模板 `--check-prefix=OBJ`+`=ASM`）；把 `disassembly.s` 15 组用例**迁移**后**删除**；`basic-encoding.s` 统一为强模板；`triple-smoke.s` 不动。**预检订正 6 处（P1–P6）**：范围「新增 vs 迁移」裁定、排除 RA 操作形式（属 `011t`）与 `010t` 命名、**独立 oracle 改为复用 `tools/llvm/test_encoding_oracle.py`**（原 #6「手推」为自证）、实测环境（build 存在 ⇒ lit 可跑；基线 3 tests/3 passed）、实测语法（`add.uo` 4 操作数、`illi` 1 操作数）、差异条表述订正。**engineer 第 1 版有覆盖缺失且完成区不实**（`add.si rd8, -1` 未迁移，对照表写「covered by rd8,1 pattern」却结论「缺失项：0」；并含错误「新发现」称 `ext.uo` opcodes.yaml 有歧义——实为 **orrr** ha=0x10 与 **orri** ha=0x18 两个不同 insn）→ 主会话实测证伪后退回；第 2 版补回 `add.si rd8, -1`（`59 23 ff ff`）并更正。**reviewer 独立验收 Accepted**：覆盖**脚本化逐条**核对零缺失（原 `disassembly.s` 17 条 + 原 `basic-encoding.s` 7 条 + 4 场景全保留）、lit **16/16**、oracle **57/57**、**抽验 7 条字节亲自从 `opcodes.yaml` 手算全一致**、反例门控（注入→FAIL、还原→PASS、oracle 注入亦 FAIL）、排除项/约束核对。**非阻断**：oracle 57 条中 7 条重复（unique 50），建议后续去重。 | engineer + reviewer |
| 2026-09-21 | **`LLVM-010t` 关闭（用户裁定）**。**预检确认该任务已无独立交付物**：①「验证 `swym`/`illi`/`fence` 可汇编」——`swym`/`illi` 已由 `008t` 的 lit 覆盖，`fence` 两侧缺口（LLVM lit + QEMU 实现）已按用户裁定入 `deferred`；②「修正 smoke `.s`」——v5 **没有**可修的汇编 smoke（`triple-smoke.s` 是 `llvm-mc --version` 版本注册冒烟），E2E 冒烟属 **`INTEG-002t`**；③「`.s → .o → .bin` 与 golden diff」——`llvm-objcopy` **未构建**（Makefile ninja 目标仅 `llvm-mc llvm-objdump FileCheck LLVMDADAOCodeGen`），且字节验证已由 `008t` 的 lit + oracle（57/57）承担；④「0628 助记符映射表」——v5 无对应旧文件，无源可修。**处置**：删除 `LLVM-010t-系统指令助记符与smoke修正.md`（编号留空、不重编）；同步 `LLVM-001k`（任务表/依赖链/说明）、`LLVM-015m`（关联任务/产出清单）、`milestones.md`、`LLVM-008t` 历史引用；理由入 `deferred.md`。 | 主会话 + 用户 |
| 2026-09-21 | `LLVM-011t`：**RA 指令 lit 补齐 + 复验**（新建 `tests/lit/MC/Dadao/ra.s` 11 用例：6 条 RA 指令 + 边界 `ra0`/`ra63`/`immu6=0`/`immu6=63`；扩展 `tools/llvm/test_encoding_oracle.py` 57→68）。**预检订正 5 处（P1–P5）**：①RA 的 TableGen/AsmParser/CodeEmitter/Disassembler **已由 `005t`/`006t`/`007t` 完成**（def 在 patch `0004`）⇒ 范围缩窄为「lit 补齐 + 验证」、**不改补丁**；②消歧**已实现方案 B**（AsmName=`"ld.o"` + 寄存器类消歧）且**与 `contract-isa §4.9` 一致**，任务书原「推荐方案 A（`ld.o-ra`）」偏离 spec、作废；③删除错误的 `set.rd` 条（`set.rd` 不在 `opcodes.yaml`，spec 中为「装载立即数到 RD」伪指令，与 RA↔RD 复制无关）；④实测基线 6 条 RA 字节 + round-trip；⑤唯一真实缺口 = lit 零 RA 覆盖。**reviewer 独立验收 Accepted**：lit **17/17**、oracle **68/68**、6 条复验逐字节吻合、**全 11 条 indep==llvm-mc==ra.s 三方比对 0 mismatch**、反例门控（注入→FAIL、sha256 还原→PASS、oracle 注入亦 FAIL）、`components/` diff 空、无别名、其余 lit 未改。 | engineer + reviewer |
| 2026-09-21 | `LLVM-012t`：**lit 字节 oracle**（新建 `tools/llvm/check_lit_bytes.py`：解析 `tests/lit/MC/Dadao/*.s` 的 `# OBJ:` 手写字节 ↔ `contracts/opcodes.yaml` 的 mask/value 独立校验；**纯 Python + yaml、不运行 LLVM 工具**；`N == 独立宽松计数` 才 exit 0）。**预检订正 6 处（P1–P6）**：mnemonic 提取须遇 `{{` 即停（实测 `\S+` 会吞掉 FileCheck 模式与操作数）、`N` 下限改**独立计数**（原自指、防空绿失效）、补**反例门控**三组、明确与 `test_encoding_oracle.py` 的**互补边界**（后者校验 llvm-mc 实际输出、不解析 lit）、实测基线（**41 条 `OBJ:` 行 41/41 唯一匹配 + mnemonic 全一致**）、删除同值笔误条。**成果**：`41 patterns OK` / exit 0；反例三组（错字节/错 mnemonic/空 `OBJ:` 集）均 exit 1；大写 hex 注入由「N≠独立计数」兜住（纵深防御）。**reviewer 独立验收 Accepted**（三组反例 + 大写兜底路径独立复现、独立计数用两种方法复算 41、只读性/无 LLVM 调用核对）。**非阻断**：无 `--lit-dir` 选项；独立计数不能捕获整行 `OBJ:` 被删 → 已登记 deferred。 | engineer + reviewer |
| 2026-09-21 | **`LLVM-015m` 置「里程碑」（用户确认）——llvm 模块 M1 完成**。核验：关联任务 `001k`~`008t`/`011t`/`012t` 全部 `已验证`（`010t` 已关闭）；产出文件全存在（`adr-0006`、`components.lock.toml`、patch `0001`–`0006`+`series`、`lit.cfg.py`、17 个 lit `.s`、`check_lit_bytes.py`）；`make build-mc` PASS；`llvm-lit` **17/17（0 failures）**；`check_lit_bytes.py` **41 patterns OK** exit 0；额外 `test_encoding_oracle.py` **68/68**。跨模块影响（`fence`、`.o→.bin`）已入 `deferred`（处置完毕）；`INTEG-002t` 属 integ 模块、非本门槛。核验记录写入任务书。 | 主会话 + 用户 |
| 2026-09-21 | `TESTCASES-010t`：**数据级覆盖率缺口消解（第一批，114 缺口）**。补 `reg-arith`（45 legality + 2 boundary）/`reg-logic`（16）/`reg-shift-extend`（40）/`reg-compare`（11）全部为 **active case**；**给 `generate_isa_vectors.py` 新增 legality 生成能力**（原 0 支持；镜像 `generate_mem_vectors.py`/`generate_ctrl_jump_call_ret.py` 风格）+ 溢出 boundary。**成果**：目标 4 文件 gap **47/16/40/11 → 0**（整体 154 → **40**，余属 `011t`）；`git diff` **纯新增 1610/0**；`009t-audit.py` 320/322 0 mismatch；`make check` PASS。**reviewer 两轮**：第 1 轮 **Needs Revision**——独立全量重算发现 **3 条 legality 规则归属错误**（`add.si-rb`/`rela.si-rb` 应引 `rb_dest_rb0` 却引 `rd_dest_rd0`；`cmp.uo-rb` 目的字段是 `rdhb`(rd bank) 应引 `rd_dest_rd0` 却引 `rb_dest_rb0`）+ 完成区「坑」#4 错误陈述 + 自验方法过浅；engineer 改为**按 `opcodes.yaml` dst 字段 bank 路由**（弃 insn 名子串）；第 2 轮 **Accepted**（三方一致性 112 条 **0 不一致**、分布 **102/6/4**、生成器重跑 6 文件 byte-identical、反例门控 3/3）。**另修**：第 1 版生成器会覆盖 `reg-cond-assign.yaml` 丢失 `009t` 手工 deferred overlap case（靠 `git checkout` 绕过）→ 已把 `gen_cs_overlap_deferred()` 纳入生成器 + 修子串匹配 bug（`"cs.n" in "cs.ne-rd"`）。 | engineer + reviewer |
| 2026-09-21 | `TESTCASES-011t`：**数据级覆盖率缺口消解（第二批）+ validator 修复 + 门控转严**。补 `cs.*`(5 legality)、`reg-imm-block`(13 legality + 6 overlap)、`br.*`(10 boundary)、`jump-iiii`(1 boundary)；`inventory.md` 中 `br.*`/`jump-iiii` 的 `legality ✓→—`、`boundary →✓`；**修 `validate_vectors.py`**（`deferred <reason>` 不计缺口 + gap 报告转 **`sys.exit(1)`** 阻断 + boundary 允许 `UNMAPPED`）；**给 `generate_ctrl_br.py`/`generate_ctrl_jump_call_ret.py` 新增 boundary 生成能力**。**成果**：**整体 gap 0**（747 cases）、`exit 0`；`009t-audit.py` 326/328 0 mismatch；`make check` PASS；`git diff` 纯新增 584/0。**用户裁定**：块赋值 overlap 采 **spec/QEMU 顺序语义**（`contract-isa.md:467`「序号递增逐对处理，每对先读后写」）⇒ **同步修 `009t-audit.py` 的块赋值重算**（原快照语义与错误向量同源同错，其「0 mismatches」曾不构成语义证据）。**reviewer 两轮**：第 1 轮 **Needs Revision**（6 项：4 条 `br.*` boundary 因条件不符**未 TAKEN**、2 条块赋值期望值错、`rd2ra`/`ra2rd` 规则 id 应 `ra_multi_immu6_zero`、跨 bank notes 不实、hex 笔误、审计快照语义）；修订后第 2 轮 **Accepted**（11 条 boundary 逐条独立 QEMU 实测 TAKEN→`0x87` + 负向对照、6 条 overlap 顺序语义逐条、三方一致性 36 条 0 不一致、审计修复未削弱判别力（注入快照错值→MISMATCH）、validator 强度核对）。 | engineer + reviewer |
| 2026-09-21 | **`TESTCASES-012m` 置「里程碑」（用户确认）——testcases 模块 M1 完成**。核验：关联任务 `001k`~`011t` 全部 `已验证`；产出文件全存在（`schema.md`/`inventory.md`/`README.md`/`isa/` 15 文件/`validate_vectors.py`/`Makefile`）；**数据级覆盖 gap = 0**（747 cases，exit 0——149 缺口由 `010t` 114 + `011t` 35 消解）；`009t-audit.py` 326/328 0 mismatch；`make check` PASS。跨模块：修订 `009t-audit.py` 块赋值重算为顺序语义（用户裁定）。 | 主会话 + 用户 |
| 2026-09-21 | `INFRA-010t`：**issue registry（M1-gate）**。新建 `docs/issues.yaml`（**74 条**：66 open / 8 closed，从 `.tao/knowledge/deferred.md` 编撰；含内联项 **C-27** = `ISS-074`）+ `tools/infra/check_issues.py`（fail-closed：`issues.yaml` 缺失 → exit 1）+ 接入 `make check`。**预检订正 4 处（P1–P4）**：输入源 `docs/open-spec-issues.md` **不存在** ⇒ 内容改从 `deferred.md` 编撰（用户裁定）；**`M1-gate` 此前全仓无定义** ⇒ 显式定义 = 「直接阻断**任一模块 M1 里程碑**」；**`make check` 允许变红**（用户裁定，不得为保绿错分类）；fail-closed 保留。**结果**：**M1-gate 阻断项恰 1 条 = `ISS-056`（`fence` 实现缺失 → 阻断 `QEMU-021m`）**；`check_issues.py` exit 1；**`make check` 红（exit 2）**——预期且已显式说明；移除该阻断项可绿（reviewer 临时验证）。**reviewer Accepted**（自写 validator：0 字段违规、74 条逐条溯源命中、内联项扫描确认 C-27 为唯一遗漏且已补、M1-gate 逐条判据漏判/误判 0、反例门控（含 closed 项加 `M1-gate` 不误报、yaml 非 list、文件缺失 fail-closed）、`make check` 红因与可绿证明）。**非阻断 finding 已修**：F1 完成区 bullet 计数口径（73/74/82）；F2 `issues.yaml` 头注释 `scope` 值补 `spec`。 | engineer + reviewer |
| 2026-09-21 | `INFRA-011t`：**spec 引用审计器**。新建 `tools/infra/check_spec_refs.py`（Check 1 引用有效性 + Check 2 无引用规范断言，fail-closed）+ Makefile **独立**目标 `make check-spec-refs`（**不并入** `make check`）。**首轮审计（真实）**：586 引用；命中 **标题=566 / 粗体=12 / 正文=3**；失败 **5**（全为模板占位符/文档示例：`§章节名`×3 + `§3.5` + `SimRISC-0X`×2）；Check 2 命中 **52**；总 **57 violations**，**exit 1**。**预检订正 5 处（P1–P5）**：审计对象实为 **4 个** `contract-*.md`（含模板 `contract-authoring.md`，任务书旧文不成立）；**Check 1 口径改为「标题 + 粗体引子 + 正文行」三类**（原「仅 `#{1,4}` 标题」过窄，会误报 4–5 条「内容存在但非标题」）；`(Lnn)` 实测 0 处；节标题内含 `]` 须防截断；Check 2 启发式不得压计数。**reviewer 三轮**：第 1 轮 Accepted（含 F1 tier③ 全文子串过松、F2 `SimRISC-0X` 漏检、F3 完成区文档）；engineer 第 1 次修**过头**（粗体 12→8，误杀正当命中 `§版本`）→ 主会话指出后精确修（exact 或 `<section>：` 前缀）；第 2 轮 Needs Revision（tier③ 仍为裸前缀 ⇒ `§数据`/`§指令` 假命中）→ 按 reviewer 已验证修法收敛为「行首 lead-in」；第 3 轮 **Accepted**（6 项逐条、假绿抽查 6/6、计数不变）。**N1**（`_LEAD_IN_CHARS` 含 `如`/`下` 单字）由主会话按 reviewer 处方收紧，复跑逐项一致；**N2** 返工经过补记于任务书。 | engineer + reviewer |
| 2026-09-21 | `INFRA-012t`：**spec drift 检查**。新建 `tools/infra/check_spec_drift.py`（fail-closed：枚举 `contract-*.md` → 强制分类 spec-sourced（版本头 ↔ `README.md` 版本表 + `spec/` 文件存在）/ ADR-sourced（ADR 文件存在 + `**状态**：Accepted`）/ 否则 ERROR）+ 接入 `make check` + 4 个负测试。**预检订正 4 处（P1–P4）**：**显式排除名单**排除无来源头的模板 `contract-authoring.md`（其余 3 个强制分类，不得用宽泛跳过 ⇒ 会退化为 fail-open）；**ADR 状态按中文 `**状态**：Accepted` 解析**（仓库无英文形态）；**版本表映射显式定义**（`SimRISC-0x`→`SimRISC`、`DADAO-11/21`→`AEE / ABI` 等）；`make check` 红因须区分既有 `ISS-056` 与本任务。**engineer 第 1 版 4 个负测试中 2 个失效**（`source_missing`/`source_bad_format` 判定被放在分类**之后** ⇒ 对有版本头的合约永不触发、exit 0 ✗），且汇报未给证据 → 主会话实测证伪后退回；第 2 版把 `test_mode` 判定**前置**到函数入口。**reviewer Accepted**：正向 PASS/exit 0（3 合约分类 + 映射依据）、**自建 fixture 独立证明真实 fail-closed（5 类缺陷均 exit 1）**、显式名单反证（宽泛跳过会漏报）、`make check` 红因区分如实。**F1（非阻断，已登记 deferred）**：`--test-mode` 自测判别力不足（入口无条件 return error），建议改为基于 fixture 的真实负测试。 | engineer + reviewer |
| 2026-09-21 | **`INFRA-014m` 置「里程碑」（用户确认）——infra 模块 M1 完成**。核验：关联任务 `001k`~`013t` 全部 `已验证`；**18/18 产出文件存在**；gate 状态——`manifest_check`/`check_spec_drift`/`validate_vectors` exit 0 ✓，`check_issues` exit 1（设计如此：M1-gate 阻断项 `ISS-056`）、`check_spec_refs` exit 1（设计如此：57 violations、独立目标）。⇒ **`make check` 红因唯一 = `ISS-056`（qemu 侧 `fence`）**，已按用户裁定入 `deferred` ⇒ 非本模块未处置影响。核验记录写入任务书。 | 主会话 + 用户 |
| 2026-09-21 | **`QEMU-021m` 置「里程碑」（用户确认）——qemu 模块 M1 完成**。核验：关联任务 `002t`~`019t`/`022t`/`023t` **20/20 已验证**；产出文件全存在（3 ADR、patch `0001`–`0008`+`series`、`insn.decode`、10 个 `.c.inc`、harness 全套、trans lint）；`make build-qemu` exit 0、`-M ?` 显示 `dadao-m1`；**全量 harness `747 total / 739 passed / 0 failed / 8 deferred / 0 errors`** ✓。**`fence` 处置（用户裁定「已 deferred ⇒ 不该继续阻断里程碑」）**：`fence` 的 3 条向量（`misc[3]/[4]/[5]`）标 `status: deferred` + `deferred_reason`（生成器 `generate_misc.py` 同步；`inventory.md` 3 列同步 ⇒ `validate_vectors` gap 0 ✓）；**`ISS-056` 的 `blocks` → `[]`** ⇒ `check_issues` 0 blocking ⇒ **`make check` 转绿（`repository checks: PASS`）** ✓。核验记录写入任务书。 | 主会话 + 用户 |
| 2026-09-25 | `QEMU-024t`：**`check_qemu_trans` 假绿修复**（首个**过渡期任务** `M1→M2`）。`tools/qemu/check_qemu_trans.py`：非递归 `glob("*.patch")` → 递归 `glob("**/*.patch", recursive=True)`；`collect_trans_defs()` 返回补丁计数，`n_patches == 0` → **无条件非零退出**。**缺陷**：2026-09-23 补丁集树形化后脚本扫 0 份补丁，256 条全判 MISSING 却仍 **exit 0**（假绿），`docs/m1-retrospective.md:45` 记载的 M1 门槛值 `256/256 (M1 178/178)` 已静默失效。**证据**：HEAD 版复现 `0/256 … (M1 0/178)` + exit 0；改后 `256/256 (M1 178/178)` + exit 0；反例（空目录/不存在目录 → exit 1）生效。**reviewer Accepted**（8/8 独立重跑；B1b/B2 两组反例分别证明「递归」「fail-closed」承重；完成区无夸大）。**同批约定**：确立**过渡期任务 `M<i>→M<i+1>`**（用户裁定「里程碑只是大任务的标志，不是推进的阻碍」）——`.tao/README.md` + `.tao/knowledge/milestones.md` 新增定义；`docs/m2-spec-planning.md` §6/§7-8 标注**不采纳**（不新建规范里程碑，M2 仍为「Basic CodeGen 待开始」）。**N1（转出）**：同类非递归 glob 缺陷仍在 `tools/integ/check_interface_alignment.py`（实测 80 项 PASS 57 / FAIL 23，EXIT=1）→ 已登记 `deferred.md`，归属 `integ`。 | 主会话代行 + reviewer |
| 2026-09-25 | `INTEG-005t`：**`check_interface_alignment` 同类缺陷修复**（过渡期任务 `M1→M2`）。`tools/integ/check_interface_alignment.py` 新增 `iter_patch_files()` helper（**递归** `**/*.patch` + **空补丁集 `SystemExit` 硬错误**）替换 3 处非递归 glob（原 L70/L209/L654）——2026-09-23 补丁集树形化后三处扫 0 份补丁，致 `总计: 80 项 | PASS: 57 | FAIL: 23`、EXIT 1（`INTEG-003t` 的「80/80 EXIT 0」与 `MEMORY.md` 的「80/80/0」曾静默失效）。**改后 80/80 EXIT 0 恢复**（5 ELF + 26 ADR + 41 Schema + 8 Opcodes）。**约束守住**：零期望值改动（`EXPECTED_LIT=53`/`EXPECTED_TRANS=256` 等未动）、断言总数仍 80、无 FAIL→SKIP/MANUAL 降级、L635 的 lit glob 未动、`docs/integ-interface-alignment.md` 期望表未动。**reviewer Accepted**（8/8 独立重跑；两组反例分别证明「递归」「空集硬错误」承重；完成区无夸大）。**非阻塞遗留**：①第三处调用的 `isdir` 守卫不一致（目录缺失时静默产 FAIL，但同路径的前一处无守卫仍会硬错误 ⇒ 不构成假绿）②留证「管道退出码陷阱」（`cmd \| tee; echo $?` 取到 tee 的码，致首采日志末行 `EXIT=0` 与真实 `EXIT=1` 矛盾，已重采订正）——两条均登记 `deferred.md`。 | engineer + reviewer |
